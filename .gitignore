# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# node_modules
node_modules

# Build output — regenerated by `npm run build`; deployed manually, never needed in git.
# index.html stays tracked as the Vite source entry (points at /src/main.tsx).
/dist/
/assets/
dist-ssr
*.local

# Local scratch / personal to-do notes (not part of the app)
chyby na opravu.txt

# Secrets / runtime-generated files — never commit real credentials.
config.php
**/config.php
api_key.txt
**/api_key.txt
uploads/
**/uploads/
server.md

# Throwaway maintenance scripts and raw client data — never commit; these were
# publicly served from the docroot and leaked PII (see security audit).
scratch/
tempData/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/.claude/
