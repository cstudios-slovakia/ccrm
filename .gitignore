# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# node_modules
node_modules

# Build output. The built frontend (dist/index.html + dist/assets/) IS tracked so the
# server can deploy it via `git pull` + `php ccrm update` (the publish step copies
# dist/* to the docroot; there is no Node/build step on the server).
# The root index.html stays the Vite source entry (points at /src/main.tsx); the built
# index.html lives in dist/ and overwrites the root copy at publish time.
# Everything else under dist/ (PHP copied from public/) and the published root /assets/
# are regenerated locally and stay out of git.
/dist/*
!/dist/index.html
!/dist/assets/
/assets/
dist-ssr
*.local

# public/ is the Vite publicDir (static inputs: PWA icons, manifest, sw.js, backend PHP).
# The built frontend (index.html + hashed assets/) must NEVER be committed here: the
# server's `php ccrm update` copies public/* over dist/ to refresh backend PHP, so a
# stale public/index.html + public/assets/ would clobber the freshly-built bundle and
# roll the deployed frontend back to an old version (caused the 1.6.11 save-fail regression).
/public/index.html
/public/assets/

# Local scratch / personal to-do notes (not part of the app)
chyby na opravu.txt

# Secrets / runtime-generated files — never commit real credentials.
config.php
**/config.php
api_key.txt
**/api_key.txt
uploads/
**/uploads/
server.md

# Throwaway maintenance scripts and raw client data — never commit; these were
# publicly served from the docroot and leaked PII (see security audit).
scratch/
tempData/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/.claude/

# AI-assistant context and ephemeral dev scratch notes — must not ship to the
# web docroot (repo root is the docroot on the production host).
/.agent/
AGENT_CONTEXT.md
clementineupdate.md
durianupdate.md
elderberryupdate.md
figtree.md
figtreeupdate.md
figMobileOptimise.md
