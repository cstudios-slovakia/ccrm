; Raise upload limits so document/offer attachments (images, PDFs) are not rejected.
; Honored by PHP-FPM / CGI / LiteSpeed. Default host limits (2M/8M) silently dropped
; larger files, which discarded timeline events with attachments.
upload_max_filesize = 64M
post_max_size = 80M
max_file_uploads = 30
