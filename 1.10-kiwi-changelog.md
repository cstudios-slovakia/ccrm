# Update 1.10 (Codename: Kiwi)

This document tracks all new features, architectural changes, schema migrations, and improvements introduced in the 1.10 Kiwi update cycle.

### v1.10.61-Kiwi (2026-09-21)

- **Universal Multi-Atmosphere Moving WebGL Gradient Shaders & Clean Hero UX (`FeralGradientBackground.tsx`, `FeralSkyGradient.tsx`, `LoginView.tsx`, `loginTheme.ts`, `ontologyBuilder.ts`)**:
  - **Universal Real-Time Color Morphing WebGL Shaders (`FeralGradientBackground.tsx`, `loginTheme.ts`)**:
    - Integrated extracted FeralUI WebGL fragment and vertex shaders across all four day segments with analog film grain textures:
      - **Dawn (`dawn`)**: *Madder dusk* palette (`#FFE3C7`, `#F2B8A0`, `#C77E9E`, `#7A5E9E`).
      - **Day (`day`)**: *Blue sky* palette (`#E6F2FF`, `#B3D9FF`, `#80B3FF`, `#6699E6`).
      - **Sunset (`sunset`)**: *Night sky* palette (`#B9C8F2`, `#6E7FBF`, `#4A5AA0`, `#33406E`).
      - **Night (`night`)**: *Storm light* palette (`#E8EAEE`, `#B7BDC9`, `#8C94A4`, `#596273`).
    - Engineered continuous smooth exponential lerping across shader uniforms (`u_main`, `u_low`, `u_mid`, `u_high`, `u_wind`, `u_warp`, `u_nscale`), enabling seamless color morphing during time-of-day transitions without canvas re-instantiation.
  - **Login View Hero Panel UX Polish (`LoginView.tsx`)**:
    - Cleaned up the desktop hero section by removing manual atmosphere previewer buttons, top indicator pill badges, and telemetry cards to achieve a clean, elegant layout centered on the digital clock, localized date, and greeting.
  - **SAI Swarm Ontology Node Extraction Scaling (`ontologyBuilder.ts`)**:
    - Expanded ontology knowledge graph extraction ceiling from 8–18 to 15–35+ key entities, ensuring comprehensive social actor mapping for complex community, political, and enterprise market simulations.
  - **Verification & Testing**:
    - Validated TypeScript types (`npx tsc --noEmit`) and full test suite (`npm run test:unit`).
    - Captured live browser screenshots across all 4 atmospheric modes and mobile view.

---

### v1.10.60-Kiwi (2026-09-20)

- **FeralUI Blue Sky Moving WebGL Gradient for Daylight Login Phase (`FeralSkyGradient.tsx`, `LoginView.tsx`, `loginTheme.ts`, `docs/sky.html`)**:
  - **Dynamic Moving WebGL Sky Shader (`FeralSkyGradient.tsx`)**:
    - Integrated the extracted GLSL fragment & vertex shaders from FeralUI Blue Sky gradient (`https://feralui.dev/gradients?g=2.JcpBTs...`):
      - 4-stop custom harmonic daylight palette (`#E6F2FF`, `#B3D9FF`, `#80B3FF`, `#6699E6`).
      - Real-time animated Fractional Brownian Motion (`fbm4`) with continuous wind drift (`u_wind: 0.144`), warp distortion (`u_warp: 0.235`), and noise scale (`u_nscale: 0.8675`).
      - Embedded film grain overlay extracted from `docs/sky.html` (`#sky-grain` pattern with `mix-blend-mode: overlay`).
    - Engineered performant DPR-aware rendering loop with automatic pausing when the browser tab is hidden to eliminate idle GPU/CPU usage.
    - Integrated graceful radial gradient fallback for headless or non-WebGL environments.
  - **Light-Adaptive Executive Typography (`LoginView.tsx`, `loginTheme.ts`)**:
    - Configured `isLight` support in `LoginPhaseConfig` for daylight mode.
    - Enhanced left hero typography with crisp deep-slate/navy contrast (`text-slate-900`, `text-slate-700`), frosted glass cards (`bg-white/70 border-white/80 backdrop-blur-md`), and solid royal-blue active atmosphere button.
  - **Verification & Testing**:
    - Validated all 4 time phases (Dawn, Day with Feral Sky, Sunset, Night) and mobile layout via Playwright screenshots and test suite.

---

### v1.10.59-Kiwi (2026-09-20)

- **Time-of-Day Adaptive Login Experience & Removal of Minesweeper Mini-Game (`LoginView.tsx`, `loginTheme.ts`, `loginTheme.test.ts`, `translations.ts`)**:
  - **Removed Minesweeper Mini-Game (`LoginView.tsx`, `translations.ts`)**:
    - Completely removed the 11x11 Minesweeper game grid, random mine generation algorithms, cell click/flag handlers, game status badges, deciphered success modal overlay, and obsolete translation strings.
  - **Time-of-Day Adaptive Visual Engine (`loginTheme.ts`)**:
    - Built dynamic time-phase engine calculating real-time time buckets across the day:
      - **Dawn Glow (05:00 – 08:59)**: Warm golden peach light rays (`#fde68a`), sunrise glow gradient, localized morning greetings (*Good morning / Dobré ráno / Jó reggelt!*), and inspirational momentum copy.
      - **Radiant Day (09:00 – 16:59)**: Crisp azure daylight rays (`#7dd3fc`), deep sapphire backdrop, localized daytime greetings (*Good afternoon / Dobrý deň / Szép napot!*), and enterprise acceleration copy.
      - **Golden Dusk (17:00 – 20:59)**: Fiery sunset amber rays (`#fb923c`), twilight purple backdrop, localized evening greetings (*Good evening / Dobrý večer / Kellemes estét!*), and milestone summary copy.
      - **Midnight Guard (21:00 – 04:59)**: Deep celestial starlight rays (`#818cf8`), pulsating cosmic obsidian backdrop, localized night greetings (*Good night / Dobrú noc / Jó éjszakát!*), and secure data protection copy.
  - **Executive Left-Pane Hero Section (`LoginView.tsx`)**:
    - Replaced the mini-game area on desktop (`lg:flex`) with a sleek executive command panel:
      - Dynamic time phase badge with pulsing solar/lunar beacon and phase indicator.
      - High-definition live digital clock (`HH:MM:SS`) with localized date formatting in English, Slovak, and Hungarian.
      - Dynamic localized greeting, gradient headline subtitle, and motivating slogan.
      - Three enterprise telemetry micro-cards (*Gateway Online 99.99%*, *Core Sync Engine < 1ms*, *TLS 1.3 Vault Security*).
      - Interactive atmosphere switcher dock allowing instant previewing or toggling between Dawn, Day, Sunset, Night, and Live auto modes.
  - **Responsive & Mobile Adaptation (`LoginView.tsx`)**:
    - Integrated a compact time phase greeting badge directly above the login credentials form on mobile viewports (`< 1024px`), ensuring full aesthetic adaptation on all screen widths down to 320px.
  - **Unit Testing & QA**:
    - Added unit test suite `loginTheme.test.ts` covering time phase boundaries, localization formatting, manual atmosphere overrides, and clock padding.

---

- **Comprehensive Multi-Resolution Responsive Layout Polish (868px, 410px, 375px, 320px) (`Header.tsx`, `ProjectsView.tsx`, `InvoicingView.tsx`, `WarehouseView.tsx`, `FilesView.tsx`, `RagAiView.tsx`, `SocialMediaView.tsx`, `Sidebar.tsx`, `FloatingCopilotOrb.tsx`)**:
  - **Adaptive Universal Header (`Header.tsx`)**:
    - Built interactive mobile search mode with dedicated toggle button and full-width auto-focused search overlay bar, keeping the desktop search input cleanly hidden on mobile viewports (`< 640px`).
    - Standardized utility button sizing (`h-8.5 w-8.5 xs:h-9 xs:w-9 sm:h-10 sm:w-10`) with compact icon scaling (`h-4 w-4 sm:h-5 sm:w-5`) and tight horizontal spacing (`gap-1 xs:gap-1.5 sm:gap-2.5 md:gap-4`) to eliminate header overcrowding down to 320px screen width.
    - Responsive DEMO badge collapsing to compact tag on mobile screens.
    - Responsive profile drawer max-width (`w-full max-w-[320px] sm:max-w-sm md:w-90`).
  - **Responsive Layouts Across All Core Modules**:
    - `ProjectsView.tsx`: Responsive filter grid and dropdowns (`w-full sm:w-auto sm:min-w-[130px] flex-1 sm:flex-initial`), view toggle alignment, and table horizontal scroll preservation (`min-w-[840px]`).
    - `InvoicingView.tsx`: Responsive 3-column filter grid (`grid grid-cols-1 sm:grid-cols-3 gap-2 w-full xl:w-auto`) with minimum table width (`min-w-[780px]`).
    - `WarehouseView.tsx`: Responsive table wrappers for distribution, movement, and product item tables (`min-w-[680px]` and `min-w-[840px]`).
    - `FilesView.tsx`: Datagrid table wrapper with horizontal scroll support (`min-w-[780px]`).
    - `RagAiView.tsx`: Responsive sidebar width (`w-full md:w-72 lg:w-84`) and flexible conversation list.
    - `SocialMediaView.tsx`: Horizontal scrollable tab buttons and channel switchers with compact typography (`text-[11px] sm:text-xs`).
    - `Sidebar.tsx`: Mobile bottom navigation bar with `overflow-x-auto scrollbar-none` and compact button sizing (`h-10 w-10 sm:h-11 sm:w-11`).
    - `FloatingCopilotOrb.tsx`: Dynamic bottom pad offset calculation (`bottomPad: isMobile ? pad + 64 : pad + 20`) ensuring the floating orb remains clearly visible above the mobile navigation bar without obscuring actions.
  - **Multi-Resolution Screenshot Verification (`scripts/capture-responsive-hu.mjs`)**: Automated capture and visual verification across all 21 views in Hungarian (`hu`) at 868px (tablet), 410px, 375px, and 320px (mobile).
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

### v1.10.57-Kiwi (2026-09-20)

- **Complete Voice-Driven Project Management (`realtime_session.php`, `CopilotSidebar.tsx`, `ProjectsView.tsx`)**:
  - **Comprehensive Project Management by Voice (`realtime_session.php`, `CopilotSidebar.tsx`)**:
    - `create_project`: Direct creation and registration of projects via voice commands with name, client linkage, project type resolution, allocated budget, deadline date, priority star rating, and assigned project managers.
    - `update_project`: Dynamic voice updates to existing projects matching by name or ID, supporting status progression (`new`, `active`, `completed`, `on_hold`, `cancelled`), budget revisions, deadline postponements, delay reason logging, rating adjustments, and manager re-assignment. Automatically stamps `finished_at` when marked as completed.
    - `add_project_task`: Voice creation of project-specific tasks and action items linked directly to project and lead IDs in the database.
    - `add_project_financial`: Seamless logging of direct project costs/expenses or incoming billings with planned and real amounts, issue dates, due dates, and settlement dates.
    - `add_project_milestone`: Voice scheduling of delivery phases and Gantt milestones with start dates, completion dates, and progress percentages (`proj_gantt_{type}`).
    - `delete_project`: Voice-commanded deletion of projects with cascading unlinking of tasks and dynamic data cleanup.
  - **Direct Project Navigation & Modal Opening (`ProjectsView.tsx`, `CopilotSidebar.tsx`)**:
    - Upgraded `ProjectsView.tsx` hash router to open project detail drawers directly from `#projects?id={nameOrId}`, `#projects?edit={nameOrId}`, or `#projects?project={nameOrId}` with fuzzy and exact name matching.
    - Enhanced voice navigation tool `navigate_to_entry` with support for all multilingual project route variants (`projektek`, `projekty`, `projects`).
  - **Realtime UI Sync & Toasts**: Dispatched `ccrm:reload-data` custom events and localized toast messages upon every successful voice project operation to immediately re-sync data models and update active views.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

### v1.10.56-Kiwi (2026-09-20)

- **Voice Live Screen Navigation & Voice-Driven CRM Entry Creation (`CopilotSidebar.tsx`, `App.tsx`, `realtime_session.php`)**:
  - **Comprehensive Screen & Module Navigation (`CopilotSidebar.tsx`, `App.tsx`)**: Resolved navigation failure for cashflow/finances by adding canonical route mapping and alias resolution (`finances`, `cashflow`, `penzugy` -> `financial`; `szamlak`, `faktury`, `offers` -> `invoices`; `kanban` -> `tasks`; etc.) across both Voice WebRTC tool handlers and the global hash router.
  - **Realtime WebRTC Session Tools Registration (`CopilotSidebar.tsx`)**: Configured automatic `session.update` transmission upon data channel connection, binding tools, custom voice settings, and system instructions directly into active WebRTC calls.
  - **Voice CRM Entry Creation Tools (`realtime_session.php`, `CopilotSidebar.tsx`)**:
    - `create_task`: Direct creation of Kanban tasks and deadlines via voice (*"Hozz létre egy feladatot..."*).
    - `create_lead`: Instant creation of sales pipeline leads with values and contact details (*"Vegyél fel egy új érdeklődőt..."*).
    - `add_client_note`: Seamless logging of client phone calls, memos, and timeline events (*"Írj fel egy jegyzetet a Kovácshoz..."*).
  - **Instant UI Refresh & Notification**: Dispatched `ccrm:reload-data` custom events on entry creation to immediately re-sync local data models and display toast confirmations in the client interface.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

### v1.10.55-Kiwi (2026-09-20)

- **Resilient Entity Pill Parsing & URL Whitespace Support (`markdown.tsx`, `agent_utils.php`)**:
  - **Universal Markdown Hash Link Support with Spaces (`markdown.tsx`)**: Upgraded inline markdown parser regular expression to allow internal hash URLs containing spaces (e.g. `[Apex Development - Bytový Dom](#client-Apex Development - Bytový Dom)`).
  - **Unbracketed Entity Link Normalization (`markdown.tsx`)**: Added `normalizeEntityLinks` pre-processor to automatically detect and transform bare entity references like `Name(#client-Name)` or `(#client-Name)` into standard interactive markdown pills without text repetition.
  - **Link Tag Bracket Preservation on Restore (`agent_utils.php`, `public/api/agent_utils.php`)**: Preserved outer markdown link brackets when unmasking placeholders directly preceding link destinations (`[CLIENT_NAME_X](#...`), preventing token replacement from eating the outer square brackets.
  - **Enhanced Entity Pill Display & Icons (`markdown.tsx`)**: Added comprehensive badge coloring and icon mappings across Clients, Leads, Projects, Finances/Invoices, Tasks, and Meetings with label sanitization.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

### v1.10.54-Kiwi (2026-09-20)

- **Pseudonymization Placeholder Restoration & Resilient Client Route Resolution (`agent_utils.php`, `ClientsView.tsx`)**:
  - **Comprehensive Token Restoration (`agent_utils.php`, `public/api/agent_utils.php`)**: Enhanced `restore_text` to unmask both bracketed (`[CLIENT_NAME_5]`) and unbracketed/bare (`CLIENT_NAME_5`, `EMAIL_REF_X`, `PHONE_REF_X`) tokens across markdown links and response text. This prevents links from carrying raw placeholder tokens like `#client-CLIENT_NAME_5`.
  - **Resilient Client Detail Matching (`ClientsView.tsx`)**: Upgraded client resolution with multi-tier matching (exact match, normalized match stripping punctuation and company suffixes, prefix/containment matching, and ID matching) so that any link targeting `#client-<name>` or `#client-<id>` instantly opens the client profile.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

- **Comprehensive Screen Awareness Grounding & Natural Conversational Prompting (`useCurrentScreenContext.ts`, `agent_utils.php`, `chat_rag.php`)**:
  - **Granular Screen Context Mapping (`useCurrentScreenContext.ts`)**: Added rich, detailed screen element definitions across all CRM modules (including SAI Swarm Market Simulations with hero banners, demo cards, and simulation runs; Dashboard; Leads; Clients; Projects; Finances; Tasks; Meetings; Email; Files; Automations; Social Media; Warehouse; Settings; and dynamic detail views).
  - **Natural, Direct Executive Answers (`agent_utils.php`)**: Refactored the Executive Orchestrator system prompt to eliminate rigid, repetitive boilerplate templates (*"Következő lépések: Döntés / Felelős / Időzítés"*) on every response in favor of direct, intelligent, conversational clarity.
  - **Unbiased Grounding & Direct Question Answering (`chat_rag.php`)**: Replaced domain-biased prompt instructions (which previously forced overdue invoice reporting on unrelated questions) with strict direct relevance directives ensuring the AI answers what the user actually asked while actively utilizing live screen elements.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

- **Chat RAG Parse Error Resolution & Robust Network Transport (`chat_rag.php`, `CopilotSidebar.tsx`)**:
  - **PHP Syntax Correction (`chat_rag.php`, `public/api/chat_rag.php`)**: Resolved an unclosed curly brace in the episodic decisions injection block which caused a PHP 500 parse error when executing `POST /api/chat_rag.php`.
  - **Credentials & Error Handling (`CopilotSidebar.tsx`)**: Added `credentials: "same-origin"` to all chat RAG and history requests, and implemented safe JSON text parsing to handle unexpected network responses gracefully without unhandled exceptions.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

- **Guaranteed Copilot Welcome on Open & Ultra-Visible Elevated Chatbar (`CopilotSidebar.tsx`)**:
  - **Always-Welcome on Open**: The Executive Copilot now reliably welcomes the user on open across all scenarios (new sessions, returning visits, and historical chats), differentiating between initial comprehensive introduction and subsequent informal greetings (*"👋 Hi Erik, how can I help you today? I'm currently looking at [Screen Title] with you."*).
  - **Auto-Focus on Open**: Automatically focuses the chat input field with a brief transition delay upon opening the sidebar.
  - **Ultra-Visible Elevated Chatbar**: Overhauled the bottom chatbar styling with a glowing double-width focus border (`border-purple-400/90` with `ring-purple-500/20`), sparkling AI indicator icon, prominent talk button, high-contrast gradient Send button, and live status grounding footer with keyboard shortcut hint (`↵ Enter to send`).
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

- **Personalized Copilot Welcome Greeting & High-Visibility Elevated Chatbar (`CopilotSidebar.tsx`)**:
  - **Personalized Screen-Aware Welcome Greeting**: The Executive Copilot now always welcomes the user warmly by name on opening (e.g. *"👋 Hi Erik! I am your Executive Copilot (Kiwi). I have live CRM data access and I am actively grounded in [Screen Title]. How can I help you?"* / localized into Slovak and Hungarian).
  - **High-Visibility Elevated Chatbar**: Redesigned the bottom chat input bar with a floating dock aesthetic, featuring a high-contrast container, prominent purple glowing focus border/ring, integrated quick voice-launch button, and a gradient Send button.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

### v1.10.49-Kiwi (2026-09-20)

- **Universal Floating Executive Copilot Orb & Side-by-Side Resizable Sidebar (`FloatingCopilotOrb.tsx`, `CopilotSidebar.tsx`, `useCurrentScreenContext.ts`, `markdown.tsx`, `App.tsx`, `chat_rag.php`, `realtime_session.php`)**:
  - **Floating Draggable Orchestrator Orb (`FloatingCopilotOrb.tsx`)**: Added a universal floating Blobatar orb anchored in the bottom-right corner by default. Users can freely drag and snap the orb to any of the 4 screen corners (`bottom-right`, `bottom-left`, `top-right`, `top-left`) with physics and persistent `localStorage` memory.
  - **Side-by-Side Slide-in Copilot Sidebar (`CopilotSidebar.tsx`)**: Clicking the orb transitions the Blobatar into the top-right header of a dedicated slide-in Copilot sidebar, smoothly contracting the main workspace layout on the left so both remain visible and interactive side-by-side.
  - **Left-Edge Drag Resizing**: Built an interactive drag handle allowing users to resize the Copilot sidebar between `360px` minimum width up to `window.innerWidth - 360px`, remembering user preference across sessions.
  - **Live Screen Awareness (`useCurrentScreenContext.ts`, `chat_rag.php`)**: The Copilot dynamically detects the active view/tab, sub-section, and selected record (e.g. Clients, Leads, Projects, Invoices, Tasks, Meetings, Settings) and feeds live screen context into the RAG engine and voice assistant.
  - **Interactive Entity Suggestions Deep-Linking (`markdown.tsx`)**: Extended Markdown link parsing to recognize internal hash routes and render interactive `EntityPill` chips (Clients, Leads, Projects, Invoices, Tasks, Meetings). Clicking an entity pill in the chat immediately navigates the left workspace to that entry without closing the Copilot.
  - **Embedded Voice Studio Mode with Automated Screen Navigation (`realtime_session.php`)**: Added a dedicated "Talk" button inside the sidebar that toggles an embedded live voice studio featuring the animated talking Blobatar, voice wave visualizer, real-time transcription, and automated `navigate_to_entry` tool execution when voice queries ask to open or show CRM records.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

### v1.10.48-Kiwi (2026-09-20)

- **RAG AI Chat Layout & Flexbox Overflow Containment (`RagAiView.tsx`)**:
  - **Flexbox `min-w-0` Containment**: Added `min-w-0`, `max-w-full`, and `overflow-hidden` constraints to the right workspace chat column, quick prompts bar, message bubbles, and header container. This prevents wide AI responses (such as comprehensive markdown tables) and horizontal suggestion pills from breaking flex bounds and squashing the left executive roster sidebar.
  - **Horizontal Table & Suggestion Scroll**: Ensured suggestion prompt pills and markdown tables scroll horizontally within their respective containers without expanding the parent pane width.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

### v1.10.47-Kiwi (2026-09-20)

- **Conversational Fluency & Repetitive Greeting Elimination in Live Voice Calls (`ExecutiveCallModal.tsx`, `realtime_session.php`)**:
  - **Single-Shot Greeting Decoupling**: Removed the persistent "MANDATORY FIRST ACTION (PICKUP GREETING)" directive from the main session instructions so the voice engine no longer repeats introductions or welcome messages on subsequent turns.
  - **Continuous Dialogue Directives**: Updated the system prompt to explicitly instruct the model to engage in ongoing, fluid, back-and-forth dialogue without repeating greetings or titles once the conversation is underway.
  - **Barge-in / Interruption Cancellation (`ExecutiveCallModal.tsx`)**: Added `response.cancel` dispatching upon `input_audio_buffer.speech_started` so the agent immediately ceases speaking when the user interrupts, providing seamless conversational flow.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

- **Adaptive Executive Greetings: First-Time Formal Introduction vs Returning Casual Pickup (`ExecutiveCallModal.tsx`, `realtime_session.php`)**:
  - **First-Time Interaction Memory**: Added per-agent localStorage tracking (`ccrm_agent_intro_<userId>_<agentId>`) that determines whether the user has interacted with the agent before.
  - **Returning Caller Casual Greeting**: Subsequent calls to the same executive greet the user casually and warmly by name (e.g. *"Hi Erik, how can I help you today?"*, *"Ahoj Erik, ako ti môžem dnes pomôcť?"*, *"Szia Erik, miben segíthetek ma?"*), jumping straight into problem-solving without reciting repetitive titles or credentials.
  - **First-Time Call Introduction Preserved**: First-time callers still receive the complete strategic introduction (name, executive position, and live CRM database access verification).
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

- **Pure Opaque White Radial Gradient Voice Interface (`ExecutiveCallModal.tsx`)**:
  - **Opaque White Gradient Background**: Replaced the translucent backdrop blur with a solid, 100% opaque radial white gradient (`radial-gradient(circle 1000px at 50% 45%, #ffffff 0%, #fafbfc 45%, #f0f4f9 80%, #e8edf4 100%)`), completely covering underlying dashboard elements.
  - **Elimination of Unwanted Colored Blooms & Rings**: Removed colored background blooms, soundwave discs, and border halos so the frameless Blobatar is rendered purely and cleanly on the white gradient background.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

- **Centered Frameless Voice Interface with Radiant Gradient & Intense Backdrop Blur (`ExecutiveCallModal.tsx`)**:
  - **Centered Scene**: Re-aligned the entire calling experience to the center of the viewport with the Blobatar and floating executive metadata centered horizontally and vertically.
  - **Cardless & Frameless Pure Scene**: Stripped all container cards, bounding boxes, and division panels. The frameless Blobatar, floating typography, live status indicator, audio waves visualizer, and call controls float directly in space.
  - **Luminous Radial Gradient & Deep Frosted Blur**: The entire viewport is covered with intense backdrop blur (`backdrop-blur-3xl`) featuring a pure radial white gradient radiating outward from the central Blobatar.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

- **Luminous Fullscreen Voice Calling Experience with Right-Anchored Frameless Blobatar & Talking Cadence (`BlobatarAvatar.tsx`, `ExecutiveCallModal.tsx`)**:
  - **Right-Anchored Layout**: Repositioned the live voice call stage to the right side of the screen, pairing it on desktop with a spacious frosted glass live transcript stream and executive context viewer on the left.
  - **Frameless Organic Blobatar Rendering (`BlobatarAvatar.tsx`)**: Introduced a `frameless` mode that strips all bounding borders, container backgrounds, and drop shadows from the avatar wrapper, allowing the organic SVG blob to float freely without a frame.
  - **Speech-Synced Dynamic Talking Motion (`ExecutiveCallModal.tsx`)**: Engineered an organic speech animation system using custom keyframes (`@keyframes blobTalk`) combined with a fast mouth-flap tick cycle (`speakingTick`) that squashes, stretches, tilts, bobs, and moves the Blobatar dynamically whenever `isAiSpeaking` is active.
  - **Fullscreen Radiant Frosted Glass & 30px Ambient Margin**: Transitioned the call modal from dark mode to a bright, luminous glass aesthetic with 30px viewport margins that softly blur the underlying CRM UI with a radial white gradient emanating directly from the Blobatar's coordinate.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

- **OpenAI Realtime GA Session Initialization & Pre-Bake RAG Injection Fix (`realtime_session.php`, `ExecutiveCallModal.tsx`)**:
  - **Direct Session Schema Initialization (`realtime_session.php`)**: Corrected the OpenAI Realtime `client_secrets` request schema to use `type: 'realtime'` with structured `audio.output.voice` instead of legacy parameter paths. This allows the complete 48.7KB live CRM knowledge base prompt to be validated, accepted, and embedded directly into the ephemeral session on OpenAI's servers at creation time with HTTP 200.
  - **Lightweight WebRTC DataChannel Negotiation (`ExecutiveCallModal.tsx`)**: Streamlined the `session.update` handshake over WebRTC to configure VAD and transcription without sending oversized payloads that could trigger WebRTC buffer drops.
  - **Proactive CRM Access Reference**: Updated the initial greeting directive to explicitly state active database access and cite client accounts.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

### v1.10.41-Kiwi (2026-09-20)

- **Live Realtime CRM Database Tool Calling & Client Directory Roster (`ExecutiveCallModal.tsx`, `agent_utils.php`, `realtime_session.php`)**:
  - **Master Client & Account Directory in Voice Prompt (`agent_utils.php`)**: Added the full client roster directly into the Pipeline & Client Directory overview block with company names (e.g. Cstudios, s.r.o.), IČO, status, city, owner, and opportunity value.
  - **Active Record Baseline Scoring (`agent_utils.php`)**: Prioritized active accounts, accepted/won clients, and companies with rich financial analysis and AI summaries in the RAG ranking so their detailed profiles are always available in initial voice context.
  - **Realtime WebRTC Live Function Calling Tool (`query_crm_live_data` in `ExecutiveCallModal.tsx`, `realtime_session.php`)**: Integrated native function calling into the OpenAI Realtime WebRTC data channel. The voice agent can now execute live deep searches against the 16 CRM domains mid-call and speak back answers with exact metrics.
  - **Anti-Refusal System Directives (`realtime_session.php`)**: Explicitly instructed the model that it has 100% direct, authoritative, live access to CRM data, preventing generic "I don't have access to realtime databases" refusals.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

### v1.10.40-Kiwi (2026-09-20)

- **Complete 16-Domain RAG Grounding & Live CRM Data in WebRTC Realtime Voice Calls (`ExecutiveCallModal.tsx`, `realtime_session.php`)**:
  - **Live CRM Knowledge Grounding in Voice Calls (`ExecutiveCallModal.tsx`)**: Fixed an issue where the WebRTC data channel's `session.update` was overriding and clearing the backend instructions with a generic 3-line string upon connection. Connected sessions now deliver the complete, domain-weighted RAG prompt containing live financial records, unpaid invoices, active projects, pipeline statistics, inventory movements, and executive decisions.
  - **Comprehensive Prompt Delivery (`realtime_session.php`, `public/api/realtime_session.php`)**: Added the compiled `instructions` payload directly to the `/api/realtime_session.php` initialization response and increased the domain block threshold to 32 items.
  - **Natural Spoken Pronunciation**: Configured the voice call RAG context builder to preserve actual company, client, and project names so the OpenAI Realtime voice engine speaks authentic names naturally without reading masked pseudonymization brackets.
  - **Docker Container Sync**: Rebuilt and synchronized the production Docker web container.

---

### v1.10.39-Kiwi (2026-09-20)

- **Continuous Idle Motion & Dynamic Animation Default for All Blobatars (`BlobatarAvatar.tsx`, `RagAiView.tsx`, `ExecutiveCallModal.tsx`, `QaAssistantDrawer.tsx`)**:
  - **Always-On Idle Animations (`BlobatarAvatar.tsx`)**: Set `animate="always"` as the default across the component so that all Blobatars actively perform their seeded idle motions (breathing, blinking, micro-bobbing, and eye gaze saccades) without requiring direct hover.
  - **Sidebar Roster & Message Bubble Animations (`RagAiView.tsx`)**: Enabled continuous animation for the Flagship Orchestrator card, C-Suite specialist list, custom agent entries, chat header, message bubbles, and loading indicators.
  - **Docker Image Synchronization**: Rebuilt and updated the production Docker web container to serve the live animations.

---

### v1.10.38-Kiwi (2026-09-20)

- **Vibrant Role-Colored Gradient Backdrops & Icon Enhancement for Blobatars (`BlobatarAvatar.tsx`, `RagAiView.tsx`, `ExecutiveCallModal.tsx`)**:
  - **Role-Colored Container Gradients (`BlobatarAvatar.tsx`)**: Introduced `ROLE_CONTAINER_THEMES` supporting rich, role-specific gradient containers (`purple`, `emerald`, `amber`, `rose`, `slate`, `cyan`, `orange`, `indigo`, `blue`, `council`, `gold`) that preserve the distinct color identity of each C-Suite role.
  - **Seamless Transparent SVG Rendering**: Configured `<Blobatar>` with transparent background when container themes are applied, allowing animated blob characters to sit directly on vibrant gradient backdrops.
  - **Executive Roster & Workspace Integration (`RagAiView.tsx`, `ExecutiveCallModal.tsx`)**: Passed appropriate `roleColor` props to the Flagship Orchestrator card, all C-Suite specialist avatars, custom database agents, Boardroom Council headers, chat bubbles, and the central WebRTC voice call avatar modal.

---

### v1.10.37-Kiwi (2026-09-20)

- **Interactive Blobatar Facial Expressions, Real-Time Thinking Seesaw Motion & User Speech Reactions (`BlobatarAvatar.tsx`, `ExecutiveCallModal.tsx`, `RagAiView.tsx`, `QaAssistantDrawer.tsx`)**:
  - **Full `blobatar/expression` Suite Integration**: Extended `BlobatarAvatar.tsx` to support the complete range of named expressions (`thinking`, `surprised`, `happy`, `smug`, `wink`, `unsure`, `sad`, `mad`, `sleepy`, `scared`, `love`, `shy`, `sick`, `idle`) with automatic resolution to `@blobatar/react` and `blobatar/motion.css`.
  - **Real-Time WebRTC Call Reactions (`ExecutiveCallModal.tsx`)**:
    - **Connecting State**: Avatar activates `thinking` pose with staggered eye seesaw motion and amber pulse ring.
    - **User Speaking Reaction**: Directly reacts to user microphone speech by adopting the attentive `surprised` expression with active green speech rings.
    - **AI Thinking / Latency State**: Transitions to animated `thinking` expression when the user stops speaking and the executive synthesizes advice.
    - **Executive Speaking State**: Switches to confident, authoritative `smug` / `wink` expression with pulsating radial purple soundwave halos.
  - **Live Chat Header & Message Bubble Expressions (`RagAiView.tsx`)**:
    - Header avatar live-reacts to user typing in the input box (`surprised` expression) and transitions to active `thinking` seesaw motion during response generation.
    - Added pulsating animated thinking indicator beside the active thinking avatar in loading response streams.
  - **SAI Simulation Interrogation Hub (`QaAssistantDrawer.tsx`)**:
    - Added thinking blobatars for Chief Intelligence Analyst and dynamic interview agent expressions (`unsure` / `smug`).

---

### v1.10.36-Kiwi (2026-09-20)

- **Deep RAG Access & Autonomous Execution for All Executive Personas (`chat_rag.php`, `agent_utils.php`, `cron_agents.php`, `realtime_session.php`)**:
  - **Universal Executive Agent Resolution (`chat_rag.php`)**: Unified `run_agent` execution to seamlessly resolve both default C-Suite specialist roles (CSO, CFO, CHRO, GC, COO, CMO, CPO, Boardroom Comms, Orchestrator) and user-configured custom agents.
  - **Comprehensive 16-Domain RAG Grounding in Autonomous Runs (`agent_utils.php`)**: Extended `execute_autonomous_run()` to query the full 16 CRM domains with role-targeted domain weighting, current calendar date context, and automatic privacy pseudonymization.
  - **Full Modality Parity**: Ensured complete RAG grounding across Interactive Executive Chat, WebRTC GPT-Live Voice Calls, Boardroom Deliberation Council, and Autonomous Cron Execution (`cron_agents.php`).

---

### v1.10.35-Kiwi (2026-09-20)

- **Comprehensive 16-Domain RAG Grounding & Multi-Turn Conversational Memory for Executive Agents (`agent_utils.php`, `chat_rag.php`, `realtime_session.php`)**:
  - **Executive Specialist Prompt Directives (`agent_utils.php`)**: Enhanced the core system prompts for the Executive Orchestrator and all 8 C-Suite specialists (CSO, CFO, CHRO, GC, COO, CMO, CPO, Boardroom Comms) with explicit domain-specific RAG instructions to cross-reference live CRM figures, calculate unit economics from actual invoices, pinpoint bottlenecked projects, and inspect custom registry expirations.
  - **Multi-Turn Conversational Thread Memory (`chat_rag.php`)**: Preserved up to 8 recent turns of sanitized dialogue history in the LLM context window for each executive agent and 4 turns for Boardroom Council deliberations, enabling fluid multi-turn strategic inquiries, follow-ups, and Q&A.
  - **Live Grounding Synchronization**: Synchronized all backend updates between `public/api/` and `api/`, and verified against the Docker container backend.

---

### v1.10.34-Kiwi (2026-09-20)

- **Deterministic Geometric Blobatar Avatars for All Executive & CRM Chats (`BlobatarAvatar.tsx`, `RagAiView.tsx`, `ExecutiveCallModal.tsx`, `QaAssistantDrawer.tsx`, `index.css`, `package.json`)**:
  - **Blobatar Integration (`@blobatar/react`, `blobatar`)**: Integrated the zero-dependency deterministic geometric avatar system with idle animation CSS support (`blobatar/motion.css`).
  - **Reusable `BlobatarAvatar` Component (`BlobatarAvatar.tsx`)**: Created modular, theme-tailored avatar wrapper supporting deterministic name seeding, customizable sizing, rounded corners (`md`, `lg`, `xl`, `2xl`, `full`), status badges, and interactive hover & continuous idle motions.
  - **Dynamic C-Suite Executive & User Persona Avatars (`RagAiView.tsx`)**:
    - Sidebar cards: Flagship Executive Orchestrator, C-Suite specialists (CSO, CFO, CHRO, GC, COO, CMO, CPO, Boardroom), and custom user-created agents all feature unique geometric blobatars seeded by their persona name.
    - Workspace Chat Header: Active executive and Boardroom Council deliberation hub dynamically display their corresponding blobatars.
    - Chat Message Streams: Messages from executives, boardroom council deliberations, and the authenticated user are styled with individual blobatars.
  - **Live WebRTC Voice Call Modal Avatars (`ExecutiveCallModal.tsx`)**:
    - Central pulsing executive avatar powered by animated Blobatar that breathes and reacts dynamically when AI or user speaks.
    - Live transcript turns with speaker-specific Blobatar badges.
  - **SAI Simulation Interrogation Hub (`QaAssistantDrawer.tsx`)**:
    - Distinct blobatars for Chief Intelligence Analyst and simulated market specialist interview agents.

---

### v1.10.33-Kiwi (2026-09-20)

- **Comprehensive 16-Module RAG Knowledge Engine & Streamlined Voice Call Action UI (`RagAiView.tsx`, `agent_utils.php`, `chat_rag.php`, `realtime_session.php`)**:
  - **Universal 16-Domain RAG Knowledge Engine (`agent_utils.php`)**: Engineered `build_comprehensive_crm_rag_context()` spanning all 16 CRM domains (Pipeline/Leads, Clients, Projects, Tasks/Kanban, Invoices/Offers, Financial Management/Cashflow, Warehouse/Inventory, Meeting Transcripts, Custom Registries, Emails, SAI Swarm Simulations, Workflows, Team Roster/Audit Logs, Episodic Memory, Social Media, and Custom Dashboards) with role-specialized boosting and intelligent intent filtering.
  - **Shared Live Grounding Across All Modalities**: Fully wired the comprehensive RAG engine into Text Chat (`chat_rag.php`), Multi-Agent Boardroom Council Deliberations (`convene_council`), Autonomous Agent Checks (`cron_agents.php`), and Live WebRTC Voice Calls (`realtime_session.php`).
  - **Streamlined Voice Call Action UI (`RagAiView.tsx`)**: Placed a prominent "Call [Assistant Name]" action button on the left of the chat input field with multi-language localization (SK: "Zavolať ...", HU: "Hívás: ...", EN: "Call ..."), and decluttered the top-right header by removing redundant buttons in favor of a clean Reset Chat control.

---

### v1.10.32-Kiwi (2026-09-19)

- **GPT-Live Realtime WebRTC Voice Calling for All Executive Personas & Orchestrator (`ExecutiveCallModal.tsx`, `RagAiView.tsx`, `realtime_session.php`, `agent_utils.php`, `defaultExecutives.ts`)**:
  - **Bidirectional WebRTC Realtime Audio Engine**: Built dedicated low-latency voice call modal connecting directly to OpenAI Realtime API (`gpt-4o-realtime-preview-2024-12-17`) over WebRTC audio media stream with secure server-minted ephemeral client secrets.
  - **Distinct Executive Voice Cast**: Assigned distinct OpenAI realtime voices for each C-Suite role (`alloy` for Flagship Orchestrator, `ash` for CSO Strategy, `sage` for CFO Finance, `coral` for CHRO People & HR, `echo` for General Counsel & Legal, `verse` for COO Operations, `shimmer` for CMO Marketing, `verse` for CPO Product, `ballad` for Boardroom Communications).
  - **Proactive AI Pickup Greeting**: Configured the executive to proactively pick up the call first, greeting the user warmly by name in their language (e.g. *"Dobrý deň Erik"* / *"Hello Erik"*), introducing their role, and asking what strategic dilemma to discuss.
  - **Live Audio Waveform Visualizer & Call Timer**: Interactive radial sound wave visualizer reacting dynamically to audio levels, live microphone mute/unmute control, elapsed call duration counter, and instant hangup.
  - **Live Real-time Transcript & Chat Persistence**: Real-time transcript drawer capturing both user and AI audio transcriptions via WebRTC DataChannel events (`response.audio_transcript.delta`, `conversation.item.input_audio_transcription.completed`), automatically appending the call record to the executive's chat thread upon hanging up.
  - **1-Click Call Buttons & Custom Agent Voice Customization**: Added direct voice call buttons on the Flagship Orchestrator card, specialist sidebar items, and active chat header, with customizable Realtime voice selectors in Create & Edit Agent modals.

---

### v1.10.31-Kiwi (2026-09-19)

- **OpenExecutive Multi-Agent C-Suite Architecture & Boardroom Council Deliberation (`RagAiView.tsx`, `executivePrompts.ts`, `defaultExecutives.ts`, `agent_utils.php`, `chat_rag.php`)**:
  - **Flagship Executive Orchestrator**: Pinned prominent purple/gold C-Suite orchestrator card with direct 1-click chat routing, synthesis mode, and strategic briefing capabilities.
  - **Direct 1-Click C-Suite Roster**: Added dedicated high-specialization executive personas for Strategy (CSO), Finance (CFO), People & HR (CHRO), Legal & Risk (GC), Operations (COO), Marketing & Growth (CMO), Product (CPO), and Boardroom Communications.
  - **Executive Council Boardroom Deliberation Mode**: Multi-agent consensus engine (`convene_council`) querying multiple C-Suite executives in parallel across their specific domains and synthesizing cohesive strategic boardroom verdicts with consensus points, trade-offs, and immediate action items.
  - **Independent Per-Agent Chat Threads & Reliable Persistence**: Preserved multi-agent conversation threads with in-memory caching and automatic fallback to primary CRM database (`$pdo`) when vector DB is inactive, ensuring chat histories are never reset when switching between executives.
  - **Episodic Strategic Decision Memory (`rag_decisions`)**: Persisted executive decisions and boardroom consensus in SQLite / MariaDB with automatic `<past_decisions>` injection into RAG context to preserve strategic continuity across sessions.
  - **Dynamic Strategic Prompt Chips & Skill Inspector**: Context-aware prompts tailored for each C-Suite role plus on-the-fly markdown skill inspection and editing.

---

### v1.10.30-Kiwi (2026-09-18)

- **Simultaneous 1.9-Jackfruit Feature & Fix Synchronization from Main (`main` -> `1.10-kiwi`)**:
  - **Full 1.9 Release Integration**: Merged all parallel 1.9 updates (through v1.9.82) into `1.10-kiwi`, keeping `main` as the stable 1.9 production branch and `1.10-kiwi` as the forward-looking 1.10 branch.
  - **Invoices & Price Offers (`InvoicingView.tsx`, `invoiceFinanceBridge.ts`, `documentNumbering.ts`)**: Integrated the entire 1.9 Price Offers and Invoices module with custom AI templates and external SuperFaktura/iDoklad support.
  - **Company Registry Lookup (`companyRegistry.ts`, `CompanySuggestions.tsx`, `useCompanyLookup.ts`)**: Unified auto-completing registry lookup for sole traders and corporate entities across billing, suppliers, and client creation.
  - **Modular Dashboard & Custom Widget System (`dashboard/`, `dashboardWidgets.ts`, `dashboardGrid.ts`)**: Drag-and-drop grid dashboard with widget settings, KPI cards, and pipeline stage filters.
  - **Theme System (`ThemeSettings.tsx`, `theme.ts`, `color.ts`)**: Light, Dark, System, and Sunrise-based auto-switching theme architecture with semantic CSS variable tokens.
  - **Project Management Enhancements (`ProjectDetailsView.tsx`, `ProjectTasksPanel.tsx`, `ProjectListViewMenu.tsx`, `projectSort.ts`, `projectColumns.ts`)**: Added inline tasks, project list view configurations, customizable project attributes, and client pairing.
  - **Financial Management & Cashflow Overviews (`FinancialManagementView.tsx`, `financialCategoryTree.ts`, `futureMovements.ts`, `financialRecordMerge.ts`)**: Drag & drop category trees, future cashflow projections, and ledger reconciliations.
  - **Autonomous QA Test Suite (`tests/e2e/`, `scripts/qa/`)**: End-to-end testing crawler, Playwright specs, regression canaries, and diagnostic recorders.

---

### v1.10.29-Kiwi (2026-09-18)

- **Version Codename Alignment (`src/utils/version.ts`, `1.10-kiwi-changelog.md`)**:
  - **Codename Alignment to Kiwi**: Formally aligned the 1.10 version codename to **Kiwi** (preserving alphabetical sequence: Grapefruit 1.6, Huckleberry 1.7, Imbe 1.8, Jackfruit 1.9, Kiwi 1.10).
  - **Branch Preservation**: Retained `1.10-kiwi` as a dedicated branch for all 1.10 features and enhancements.

---

### v1.10.28-Kiwi (2026-09-13)

- **Model Mapping Alignment & Fail-Safe Swarm Persona Batching (`personaSynthesizer.ts`, `ontologyBuilder.ts`, `reportSynthesizer.ts`, `llmProxyClient.ts`, `public/api/swarm.php`, `api/swarm.php`)**:
  - **OpenAI Model Name Mapping in Server Gateway (`public/api/swarm.php`, `api/swarm.php`)**:
    - Synchronized model aliasing across both API directories so internal branding models (`gpt-5.6-luna` and `gpt-5.6-terra`) reliably map to live production endpoints (`gpt-4o-mini` and `gpt-4o`).
    - Increased server-side default token limit to 8000 tokens.
  - **Micro-Batch Persona Synthesis (`personaSynthesizer.ts`)**:
    - Partitioned persona generation into compact, ultra-fast parallel batches of 10 agents with 6000 tokens per batch.
    - Added resilient structured fallback generator for missing agents, guaranteeing persona generation never throws errors.
  - **Token Headroom & Fallback Resilience in Ontology and Report (`ontologyBuilder.ts`, `reportSynthesizer.ts`)**:
    - Raised ontology token ceiling to 8000 tokens and strategic report token ceiling to 10000 tokens with automatic structured fallback builders.
  - **Auto-Retry on Token Length Exhaustion (`llmProxyClient.ts`)**:
    - Added automatic retry with doubled token headroom if OpenAI returns `finish_reason: length`.

---

### v1.10.27-Kiwi (2026-09-13)

- **Smart Simulation Navigation & Card Interaction Fix (`SaiModule.tsx`)**:
  - **Unstarted Simulation Direct Configuration Routing**:
    - Simulations with `status === 'draft'` or `status === 'prepared'` with `current_round === 0` and no posts are now treated as unstarted rehearsals.
    - Clicking their card in the `#sai` list view directly opens the simulation configuration view (`CreateRehearsalView`) with all parameters (title, hypothesis, seed document, swarm scale, rounds, CRM sources, context documents) pre-filled and ready for execution.
    - Updated card status badge to clearly indicate "Piszkozat" (Draft) or "Előkészítve" (Prepared) with amber accents and `Bookmark` icon.
    - Updated card bottom action link to "Konfiguráció szerkesztése & Indítás" (Edit Config & Launch).
    - Added full keyboard accessibility (`role="button"`, `tabIndex={0}`, Enter / Space activation) and click responsiveness.
  - **Fail-safe Route Redirection**:
    - In `handleOpenPastSimulation`, if an unstarted simulation without checkpoint posts or report is opened, it automatically redirects into `handleOpenDraft` rather than rendering an idle empty war room.

---

### v1.10.26-Kiwi (2026-09-13)

- **Comprehensive Token Headroom Expansion Across Full Simulation Pipeline (`ontologyBuilder.ts`, `personaSynthesizer.ts`, `simulationLoop.ts`, `reportSynthesizer.ts`, `api/swarm.php`)**:
  - **Increased Simulation Loop Agent Turn Tokens (`simulationLoop.ts`)**:
    - Raised agent turn token ceiling from 300 to 1500 tokens (`maxTokens: 1500`) to completely eliminate `finish_reason: length` during live multi-agent deliberation.
  - **Persona Synthesis Batching Optimization (`personaSynthesizer.ts`)**:
    - Reduced batch partition to 20 agents per parallel call (3 parallel batches of 20 for 60 agents) with `maxTokens: 5000` per batch.
  - **Ontology & Strategic Report Expansion (`ontologyBuilder.ts`, `reportSynthesizer.ts`)**:
    - Allocated `maxTokens: 5000` for knowledge graph extraction and `maxTokens: 6000` for final ReAct strategic briefing synthesis.
  - **Server Proxy Default Token Allocation (`api/swarm.php`, `public/api/swarm.php`)**:
    - Raised server-side default completion token fallback to 4000 tokens.

---

### v1.10.25-Kiwi (2026-09-13)

- **Batched Swarm Persona Synthesis & Token Expansion (`personaSynthesizer.ts`, `llmProxyClient.ts`)**:
  - **Batched Parallel Persona Generation (`personaSynthesizer.ts`)**:
    - For large swarms (e.g. 60 personas), splits the profile generation into concurrent batches of up to 30 agents to prevent LLM token limit truncation (`finish_reason: length`).
    - Increased `maxTokens` allocation to 4000 per batch with dedicated JSON validation.
  - **Enhanced Content Fallback & Refusal Handling (`llmProxyClient.ts`)**:
    - Increased default LLM proxy token budget from 1500 to 4000 tokens.
    - Added fallback inspection for `choice.text`, detailed error reporting with `finish_reason`, and model refusal message extraction.

---

### v1.10.24-Kiwi (2026-09-13)

- **Auto-Healing LLM Parameter Recovery & Live War Room Re-run Controls (`LiveWarRoom.tsx`, `SaiModule.tsx`, `api/swarm.php`)**:
  - **Auto-Healing Parameter Retry Engine (`api/swarm.php`, `public/api/swarm.php`)**:
    - Implemented instant auto-recovery when OpenAI returns parameter mismatch errors: if a model rejects custom `temperature` values (e.g. `Unsupported value: 'temperature' does not support 0.3`), the server automatically strips `temperature` and retries without dropping the request.
    - Implemented automatic fallback for `response_format` and token limits.
  - **In-Place Re-run & Draft Edit Controls in War Room (`LiveWarRoom.tsx`, `SocialFeedStream.tsx`)**:
    - Added dedicated **"Indítás / Újrafuttatás" (Launch / Re-run)** and **"Konfiguráció szerkesztése" (Edit Config)** buttons in the War Room HUD and empty stream placeholder when a simulation is paused or interrupted.
    - Added `handleRestartCurrentSimulation` and `handleEditActiveDraft` in `SaiModule.tsx` to seamlessly resume or re-launch simulation runs.

---

### v1.10.23-Kiwi (2026-09-13)

- **Fixed OpenAI API `max_completion_tokens` Compatibility (`api/swarm.php`, `llmProxyClient.ts`)**:
  - Replaced deprecated `max_tokens` with `max_completion_tokens` in the server-side LLM proxy (`api/swarm.php`, `public/api/swarm.php`) and client (`llmProxyClient.ts`).
  - Added model-aware payload formatting: automatically omits temperature parameter for OpenAI reasoning models (`o1`, `o3-mini`, etc.) to prevent schema errors.
  - Resolves `LLM Proxy HTTP 400: Unsupported parameter: 'max_tokens' is not supported with this model. Use 'max_completion_tokens' instead.` during live swarm execution.

---

### v1.10.22-Kiwi (2026-09-13)

- **Complete Hungarian (`hu`) Localization Across SAI Simulation Suite (`#sai`)**:
  - **Fixed Binary Language Checks Across Components**:
    - Replaced binary ternary checks (`systemLanguage === 'sk' ? ... : ...`) that prevented Hungarian from displaying with a robust 3-way translation system (`tr(systemLanguage, en, sk, hu)` and helper `createTr`).
    - Extended `systemLanguage` prop propagation to previously decoupled components: `SwarmGraphCanvas`, `GuidedDemoWalkthrough`, and `QaAssistantDrawer`.
  - **Comprehensive Localization of All Views & Modals**:
    - **Dashboard & Simulation History (`SaiModule.tsx`)**: Localized simulation status badges, execution mode pills, hero banner, interactive demo launch card, empty states, and simulation history table.
    - **Simulation Navigation HUD (`SimulationStepsBar.tsx`)**: Localized all 6 simulation steps, live timer, status badges, config review modal, and action triggers.
    - **Rehearsal Configurator (`CreateRehearsalView.tsx`)**: Localized execution mode selector, all 12 CRM grounding data sources (titles, categories, descriptions, badge counts), quick template presets, title/hypothesis/seed inputs, document upload drag-and-drop zone and modal, lookback horizon, scale, rounds, and bottom action bar.
    - **MiroFish Scenario Catalogue Modal (`TemplateCatalogueModal.tsx` & `useCasesCatalogue.ts`)**: Localized header, search bar, category pills, domain tags, empty search state, and card action buttons.
    - **Preflight Resource Estimator (`PreflightEstimatorModal.tsx`)**: Localized execution mode switches, breakdown metrics, LLM pricing warnings, confirmation checkbox, and launch button.
    - **Live War Room & Swarm Canvas (`LiveWarRoom.tsx`, `SocialFeedStream.tsx`, `SwarmGraphCanvas.tsx`)**: Localized real-time status banners, sentiment distribution counters, social media feed stance badges (Támogató / Ellenző / Semleges), zoom controls, node inspector popovers, and actor category legends.
    - **Strategic Briefing & Interrogation Drawer (`StrategicReportView.tsx`, `QaAssistantDrawer.tsx`)**: Localized executive briefing tabs, vulnerability matrices, counter-strategy action plans, buyer objection rebuttal scripts, and the conversational analyst interrogation hub.
    - **Guided Process Walkthrough (`GuidedDemoWalkthrough.tsx`)**: Localized all 6 interactive guided tour steps, user vs. system action badges, simulated input typing, and playback controls.

---

### v1.10.21-Kiwi (2026-09-13)

- **Added Products, Financials, and Social Media Posts to SAI Grounding Data Sources (`#sai`)**:
  - **Expanded CRM Grounding Data Sources (12 Sources Grid) (`CreateRehearsalView.tsx`)**:
    - Added 3 critical operational data sources to the Grounding Data Sources card grid, expanding the total from 9 to 12 active sources:
      - `products`: **Katalóg produktov & Sklad** / **Products & Inventory** (SKU, catalog prices, material attributes, technical specifications, and current warehouse stock levels) with cyan `#Warehouse` category tag and `Package` icon.
      - `financials`: **Faktúry & Finančné záznamy** / **Invoices & Financial Records** (Invoiced revenues, direct project costs, payment history, client billing, and margin performance) with emerald `#Finances` category tag and `DollarSign` icon.
      - `social_media_posts`: **Príspevky na sociálnych sieťach** / **Social Media Posts & Campaigns** (Published company posts, promotional campaigns, audience reach, engagement likes/shares/comments, and sentiment) with rose `#Marketing` category tag and `Share2` icon.
    - Updated active source counters to dynamically display up to `Všetkých 12 zdrojov aktívnych` / `All 12 sources active`.
    - Seamlessly integrated with `Select all` / `Deselect all`, lookback filters (30/90/180/365 days), and simulation configuration persistence.
  - **Backend Context Extraction Engine (`CrmContextExtractor.php`)**:
    - Implemented `extractProductsContext`: queries `warehouse_items` and `warehouse_stock` for product descriptions, dimensions, price per unit, cost, current stock, and reserved quantities.
    - Implemented `extractFinancialsContext`: queries `financial_records` for client invoices, payments, expense records, dates, and amounts.
    - Implemented `extractSocialMediaContext`: queries company social media broadcasts, posts, campaign descriptions, and engagement metrics from Zernio / social campaign tables.
    - Formatted structured grounding sections into the agent cognitive context payload (`## CRM ÚDAJE O PRODUKTOCH A SKLADOVÝCH ZÁSOBÁCH`, `## CRM FINANČNÉ ZÁZNAMY A HISTÓRIA FAKTÚR`, and `## CRM PRÍSPEVKY NA SOCIÁLNYCH SIEŤACH A KAMPANE`).
    - Synchronized across `api/Swarm/CrmContextExtractor.php`, `src-php/Swarm/CrmContextExtractor.php`, and `public/api/Swarm/CrmContextExtractor.php`.

---

### v1.10.20-Kiwi (2026-09-13)

- **Comprehensive English & Multilingual Support for SAI Module (`#sai`)**:
  - **Dynamic System Language Propagation (`App.tsx`, `SaiModule.tsx`)**:
    - Connected active user language (`userLanguage`) into `<SaiModule systemLanguage={userLanguage} />` and all child views/modals, resolving the issue where SAI remained stuck in Slovak when switching language in CCRM.
    - Initialized language from `localStorage.getItem("crm_language")` and synced with user metadata.
  - **Bilingual Interface Across All SAI Views**:
    - Added comprehensive translations dictionary (`saiTranslations.ts`) covering EN, SK, and HU.
    - Fully localized header HUD and steps bar (`SimulationStepsBar.tsx`), execution mode selector cards, quick template pills, input descriptions, CRM data grounding sources, preflight token estimator (`PreflightEstimatorModal.tsx`), live War Room feed (`LiveWarRoom.tsx`, `SocialFeedStream.tsx`), and strategic report viewer (`StrategicReportView.tsx`).

- **105-Template Scenario Catalogue (MiroFish Integration) (`TemplateCatalogueModal.tsx`, `useCasesCatalogue.ts`)**:
  - **Curated Catalogue of 105 Strategic Simulation Templates**:
    - Scraped and mapped all 105 use cases from `https://mirofish.my/use-cases` into structured bilingual templates with titles, hypotheses, descriptions, seed briefing text, and recommended CRM sources.
    - Categorized into 5 distinct domains: Strategy & Org (17), GTM & Pricing (35), Customers & Rivals (30), Crisis & PR (10), and Forecasting & Prediction (13).
  - **Interactive Template Catalogue Popup Modal**:
    - Added dedicated "Browse Template Catalogue (105)" trigger with gradient styling in the quick templates bar.
    - Modal features real-time search filtering across titles, descriptions, and hypotheses, category pill filters, and domain tag filters (#Strategy, #Competition, #Pricing, #GTM, #PR, etc.).
    - 1-Click Auto-Fill: Clicking any template card instantly populates the simulation title, hypothesis, briefing document, and recommended CRM data sources, automatically closes the modal, and provides a visual confirmation toast.

---

### v1.10.19-Kiwi (2026-09-13)

- **Interactive Demo vs. Live Test Selection in SAI Module (`#sai`)**:
  - **Dynamic Execution Mode Selector (`CreateRehearsalView.tsx`)**:
    - Added an interactive execution mode selector card at the top of the simulation configuration view with 1-click toggling between **⚡ Demo test (0.00 €)** and **🚀 Živý test (Live API)**.
    - Added interactive dual-pill badges directly in the header breadcrumbs (`DEMO TEST (0 €)` vs `ŽIVÝ TEST (LIVE)`) allowing immediate switching from anywhere in the configuration.
    - Dynamically updated the bottom action bar's token/cost estimator and launch button (`Spustiť Demo test (0 €)` vs `Prejsť na odhad tokenov & Spustiť naživo (Live)`).
  - **Enhanced Preflight Resource Estimator Modal (`PreflightEstimatorModal.tsx`)**:
    - Added an interactive mode switch inside the preflight modal itself so users can change their mind right before confirming.
    - Dynamic calculation of costs, disclaimer messages, and acknowledgment checkboxes adapting to whether Demo or Live execution is selected.
  - **Runtime Orchestration Engine (`SaiModule.tsx`, `SimulationStepsBar.tsx`)**:
    - Added `activeIsDemo` state tracking to allow running real server/OpenAI simulations even when the CRM system-level demo mode is turned on.
    - Updated `handleLaunchSimulation` to inspect `config.executionMode`: live mode initiates server checkpoints, context extraction, ontology graph building, persona synthesis, and real LLM proxy calls, while demo mode routes to fast synthetic playback.
    - Updated `SimulationStepsBar` HUD badges to clearly differentiate between `DEMO TEST (0 €)` and `ŽIVÁ SIMULÁCIA`.
  - **Backend LLM Proxy Model Mapping (`api/swarm.php`)**:
    - Added model aliasing for SAI (`gpt-5.6-luna` -> `gpt-4o-mini`, `gpt-5.6-terra` -> `gpt-4o`) to ensure OpenAI requests resolve smoothly.

---

### v1.10.18-Kiwi (2026-09-12)

- **Complete Slovak Localization for SAI Module (`#sai`)**:
  - **Demo Datasets & Simulation State (`demoData.ts`)**:
    - Translated all demo knowledge graph nodes (procurement, scale-up agencies, legacy CRM competitors, SMB consultancies, EU data protection auditors) and relationship edges to natural Slovak.
    - Translated all 6 demo agent profiles: professions, cognitive bias & personality descriptions (`userChar`), public bios, and interested topics.
    - Translated all 8 demo posts and quotes (Round 1–3) to Slovak, accurately portraying realistic B2B buyer concerns, competitor attacks, and consensus crystallization.
    - Translated the demo strategic report: executive summary, all 4 analytical chapters, key vulnerabilities, actionable countermeasures, sales objection rebuttals, and recommended GTM sequence.
    - Translated demo Q&A response generators (`getDemoAnalystAnswer` and `getDemoAgentAnswer`) to provide natural Slovak answers.
  - **Simulation Engine & Orchestration Prompts (`SaiModule.tsx`, `simulationLoop.ts`, `ontologyBuilder.ts`, `personaSynthesizer.ts`, `reportSynthesizer.ts`, `chatAssistant.ts`)**:
    - Translated all simulation preparation step messages and the Cognitive Swarm Orchestration banner in `SaiModule.tsx` to Slovak.
    - Added explicit Slovak language requirements (`CRITICAL LANGUAGE REQUIREMENT`) across all LLM system and user prompts to ensure live simulation runs generate posts, personas, ontology nodes, strategic reports, and analyst Q&A answers in professional Slovak.

---

### v1.10.17-Kiwi (2026-09-11)

- **Platform Nomenclature Migration: Chitchat & Forum (`#sai`)**:
  - **Social Feed Card Badges (`SocialFeedStream.tsx`)**:
    - Replaced Twitter (𝕏) and Reddit badges with:
      - **Chitchat**: Clean indigo badge (`bg-indigo-50 text-indigo-700 border-indigo-200/80`) paired with `MessageCircle` icon.
      - **Forum**: Amber badge (`bg-amber-50 text-amber-700 border-amber-200/80`) paired with `MessagesSquare` icon.
    - Added backwards compatibility fallback so legacy or existing rows with `'twitter'` or `'reddit'` seamlessly render as Chitchat and Forum.
  - **Swarm Simulation Models & Demo Data (`types.ts`, `demoData.ts`, `simulationLoop.ts`, `personaSynthesizer.ts`)**:
    - Updated `SwarmPost.platform` and `SimulationParameters.platforms` types to include `'chitchat'` and `'forum'`.
    - Migrated all demo posts (IDs 101–108) from `'twitter'` to `'chitchat'` and from `'reddit'` to `'forum'`.
    - Updated autonomous simulation loop prompts and random platform assignment (`'chitchat'` : `'forum'`).
    - Updated persona synthesizer system prompt to reference Chitchat and Forum accounts.
  - **Backend Database Schema & Checkpoint Ingestion (`SwarmManager.php`)**:
    - Altered posts table schema from rigid `ENUM('twitter', 'reddit')` to future-proof `VARCHAR(50) NOT NULL DEFAULT 'chitchat'`.
    - Added auto-migration query (`ALTER TABLE ... MODIFY COLUMN`) in `ensureShardedTables` for backward compatibility with existing sharded simulation tables.
    - Updated platform normalization logic in `saveCheckpoint` to map incoming posts to `'forum'` or `'chitchat'`.

---

### v1.10.16-Kiwi (2026-09-11)

- **Client Projects & Deliverables Grounding Source for Swarm AI (`#sai/create`)**:
  - **Source Card UI (`CreateRehearsalView.tsx`)**:
    - Added "Klientske projekty & Zákazky" (`projects`) option with Briefcase icon to the CRM Knowledge Grounding grid.
    - Configured automatic inclusion by default alongside Leads, Clients, Objections, Competitors, Meetings, Emails, and Files.
    - Updated active sources counters in prep messages and badge headers to dynamically reflect all active sources.
  - **Backend Context Extraction (`CrmContextExtractor.php`)**:
    - Implemented `extractProjects($cutoffDate)` joining `projects`, `project_types`, and `leads`.
    - Extracted project types, active client names, contract/deal budgets, custom attribute fields (`proj_data_*`), and Gantt milestone progress (`proj_gantt_*`).
    - Formatted structured project intelligence (`--- Client Projects, Scopes & Delivery Milestones ---`) into the simulation context prompt, enabling agents to ground their deliberations in real client commitments and delivery track records.

---

### v1.10.15-Kiwi (2026-09-11)

- **Simulation Details Loading & Shard Resilience Fixes (`#sai`)**:
  - **Sanitization & Hyphenated Simulation IDs (`SwarmManager.php`)**:
    - Updated `sanitizeId` to preserve alphanumeric characters, underscores, and hyphens (`/[^a-zA-Z0-9_\-]/`), preventing ID corruption for UUIDs or hyphenated IDs.
    - Updated dynamic table prefix generation (`$prefix`) to cleanly strip hyphens (`'sim' . preg_replace('/[^a-zA-Z0-9_]/', '', $simId) . '_'`) so MySQL table naming remains strictly compliant while preserving original simulation IDs in `swarm_simulations`.
    - Fixed `saveCheckpoint` table prefix calculation to ensure hyphen stripping matches table creation.
    - Updated `listSimulations` to include `context_documents` in the returned payload.
  - **Robust Simulation Loading & Hash Navigation (`SaiModule.tsx`, `checkpointClient.ts`)**:
    - Resolved the alert `"Could not load simulation details from server."` caused by attempting to fetch non-existent server shards for demo simulations and unpopulated draft checkpoints.
    - Intercepted demo simulation IDs (`id.startsWith('demo-')`) in `syncFromHash` and `handleOpenPastSimulation` to load local demo state directly with zero network round-trips.
    - Intercepted server drafts (`details.status === 'draft'`) and automatically routed them to the draft editor (`handleOpenDraft`) with all configuration parameters restored (including `seed_document`, `crm_data_sources`, `context_documents`, `model_name`, `diurnal_cycle`).
    - Handled empty hash subviews (`#sai/running` or `#sai/report` without `id`) gracefully by falling back to demo state if demo mode is active or navigating to the simulation list.
    - Added error handling with clear Slovak error messages and automatic redirection to `'list'` instead of unmounting or leaving broken alert loops.

---

### v1.10.14-Kiwi (2026-09-11)

- **Context Document Upload (PDF & Markdown) for Swarm AI Simulations (`#sai/create`)**:
  - **Document Ingestion & File Processing (`CreateRehearsalView.tsx`, `upload.php`)**:
    - Added dedicated drag-and-drop & file picker zone on `#sai/create` right below the seed scenario memo.
    - Added support for `.pdf`, `.md` (Markdown), and `.txt` documents up to server limits.
    - Extracted Markdown (`.md`) and text (`.txt`) content client-side with zero latency via `FileReader.readAsText` and persisted on server.
    - Updated `upload.php` to support plain text extraction from `.md` and `.markdown` files alongside `.pdf` text extraction.
    - Displayed uploaded document cards with format badges (`PDF` in red, `MARKDOWN` in purple, `TEXT` in slate), file sizes (`formatBytes`), extracted text character counts, and calculated context tokens.
    - Added text inspection modal ("Zobraziť extrahovaný text" via eye button) allowing users to verify the exact text extracted before running the simulation.
  - **Dynamic Token & Cost Estimation Integration (`CreateRehearsalView.tsx`, `PreflightEstimatorModal.tsx`)**:
    - Factored attached document context lengths into real-time token estimation and price calculations (~1 token per 4 characters).
    - Displayed attached documents token contribution badge in `PreflightEstimatorModal` (`Priložené dokumenty (X súb.) ~Y tokenov`).
  - **Checkpoint & Persistence Support (`api/Swarm/SwarmManager.php`, `types.ts`, `SaiModule.tsx`)**:
    - Stored `context_documents` in simulation checkpoints and draft states, enabling resuming drafts with previously uploaded documents intact.
  - **Grounding Agents & Strategic Synthesis (`ontologyBuilder.ts`, `reportSynthesizer.ts`)**:
    - Fed extracted document contents into the Swarm Ontology Builder to ground entities, relationships, and company clauses.
    - Provided attached document briefs to the Chief Intelligence Analyst in the strategic report synthesizer, enabling agents to evaluate and cite specific contractual terms and specifications.

---

### v1.10.13-Kiwi (2026-09-11)

- **Exclusively Supported OpenAI Intelligence Models in Strategic AI (SAI)**:
  - **OpenAI-Only Model Roster (`CreateRehearsalView.tsx`, `PreflightEstimatorModal.tsx`, `GuidedDemoWalkthrough.tsx`)**:
    - Restricted intelligence model options exclusively to supported OpenAI models:
      - **GPT-5.6 Luna** (`gpt-5.6-luna`, $0.25 / 1M tokens): Default cost-optimized, ultra-fast model replacing the unsupported legacy GPT-4o / GPT-4o Mini.
      - **GPT-5.6 Terra** (`gpt-5.6-terra`, $1.50 / 1M tokens): Heavy cognitive reasoning engine tailored for deep multi-agent deliberation and strategic countermoves.
    - **Removed Unsupported Models**:
      - Dropped legacy `GPT-4o Mini` (`gpt-4o-mini`) as 4o is no longer supported and has been replaced by `GPT-5.6 Luna`.
      - Dropped `DeepSeek Chat (V3 Reasoning)` (`deepseek-chat`) to keep model execution strictly on supported OpenAI infrastructure.
    - **Updated Pricing Breakdown & Pre-Flight Estimator**:
      - Updated `PreflightEstimatorModal` to compute and display quota estimates specifically between `GPT-5.6 Luna` and `GPT-5.6 Terra`.
      - Updated `GuidedDemoWalkthrough` stage 2 metrics to display `GPT-5.6 Terra` and `GPT-5.6 Luna` pricing cards.
      - Updated Slovak contextual helper copy to explicitly guide users on selecting supported OpenAI models.

---

### v1.10.12-Kiwi (2026-09-11)

- **Comprehensive Slovak Localization of Strategic AI (SAI) Simulation Module**:
  - **Create Rehearsal View (`CreateRehearsalView.tsx`)**:
    - All parameter titles, subtitles, placeholders, and action buttons translated into Slovak ("Názov simulácie", "Strategická hypotéza / What-If premenná", "Vstupné zadanie & Text oznámenia", "CRM podkladové zdroje dát", "Časový horizont CRM dát", "Veľkosť roju", "Simulačné kolá", "Kognitívny model", "Cirkadiánny cyklus spánku", "Uložiť ako koncept", "Prejsť na odhad tokenov & Spustiť").
    - Translated all contextual guidance blocks explaining "Na čo slúži:" and "Ako sa používa:" under every input.
    - Translated all preset strategic scenario templates ("Zvýšenie cien Enterprise", "Obrana voči reakcii konkurencie", "Uvedenie novej funkcie (Samoobslužné SAI)") and CRM grounding source card labels and badges.
    - Translated price estimator bottom pill and tooltip ("Odhad ceny", "Demo zdarma", "Kliknite pre zobrazenie rozpisu tokenov").
  - **Simulation Steps Bar (`SimulationStepsBar.tsx`)**:
    - All 6 steps fully localized ("1. Scenár", "2. Predbežný odhad", "3. Načítanie dát", "4. Live War Room", "5. Syntéza stratégie", "6. Výsledky & Chatbot").
    - Localized simulation status badges ("AKTÍVNA SIMULÁCIA", "DEMO SIMULÁCIA", "SIMULÁCIA PREBIEHA", "GENEROVANIE BRIEFINGU", "PRÍPRAVA ROJU", "SIMULÁCIA DOKONČENÁ", "NOVÁ SIMULÁCIA").
    - Localized step 1 scenario popover modal ("Krok 1: Definícia strategického scenára", "Strategická hypotéza / What-If otázka", "Vstupné zadanie & Text oznámenia", "Zavrieť").
  - **Pre-Flight Estimator Modal (`PreflightEstimatorModal.tsx`)**:
    - Complete translation of quota calculations, safety notices, and confirmation checkbox ("Predbežný odhad nákladov a zdrojov", "Veľkosť roju", "Odhadované volania", "Odhadované náklady na kvótu", "Zrušiť / Upraviť nastavenia", "Potvrdiť & Spustiť").
  - **Live War Room HUD (`LiveWarRoom.tsx`)**:
    - Localized status pill, simulated time ticker ("Simulovaný čas", "CET"), stance breakdown ("Rozdelenie postojov"), viral index ("Virálna odozva"), and premature stop button ("Ukončiť skôr").
  - **Social Feed Stream (`SocialFeedStream.tsx`)**:
    - Localized feed header, auto-scroll status toggle ("Automatické posúvanie zap." / "Automatické posúvanie pozastavené"), and stance badges ("Podporujúci", "Nesúhlasný", "Neutrálny").
  - **Knowledge Graph Canvas (`SwarmGraphCanvas.tsx`)**:
    - Localized status badge ("Graf znalostí"), zoom & pan tooltips ("Priblížiť", "Oddialiť", "Resetovať zobrazenie"), bottom category legend ("Subjekty: Klient, Konkurent, Regulátor, Agentúra, Účastník trhu"), and active node explanation popover and relation lines ("Väzba:").
  - **Strategic Report View & Playbook (`StrategicReportView.tsx`)**:
    - Localized briefing badge ("Validovaný manažérsky briefing"), buttons ("Spýtať sa AI chatbota", "Výsluch simulovaných agentov", "Kopírovať Markdown"), and strategic playbook callout ("Strategický akčný plán: Ako dosiahnuť cieľ", "Odhalené kritické zraniteľnosti", "Realizovateľné protiopatrenia", "Pripravené odpovede na obchodné námietky").
  - **AI Interrogation Drawer (`QaAssistantDrawer.tsx`)**:
    - Localized analyst welcome message, conversation reset tooltip, tabs ("Spýtať sa hlavného analytika", "Výsluch agenta (1 na 1)"), quick suggestion question chips, and input placeholder.
  - **Guided Demo Walkthrough (`GuidedDemoWalkthrough.tsx`)**:
    - Localized all demo step walkthrough metadata, educational banners ("Akcia používateľa", "Činnosť systému"), interactive mock controls, and verification checklists.
  - **Module Overview & Dashboard (`SaiModule.tsx`)**:
    - Localized empty states, database loading indicator, simulation card badges ("Dokončená", "Koncept", "Prebieha"), and navigation links.

---

### v1.10.11-Kiwi (2026-09-11)

- **6-Step Interactive Process Walkthrough Bar on Real Simulations (`src/components/sai/SimulationStepsBar.tsx`, `SaiModule.tsx`)**:
  - **Identical High-Tech Stepper Design**: Replaced the static header in active simulation views (`create`, `running`, `report`) with the exact sleek, dark 6-step progress bar from the guided demo walkthrough.
  - **Full 6-Step Simulation Lifecycle**:
    1. `1. Scenario`: Scenario definition, hypothesis, and CRM grounding sources.
    2. `2. Pre-Flight`: Worst-case token spend and model budget safeguards.
    3. `3. Swarm Ingestion`: Real-time CRM context mining, knowledge graph construction, and agent persona synthesis.
    4. `4. Live War Room`: Multi-round autonomous multi-agent debate, circadian clocks, stance shifts, and social stream.
    5. `5. Strategy Synthesis`: Chief Intelligence Analyst processing simulation dialogue and compiling strategic briefings.
    6. `6. Results & Chatbot`: Executive playbook recommendations and persistent AI interrogation hub.
  - **Seamless Cross-Stage Interactivity**:
    - Clicking completed steps allows instantaneous switching between the **Live War Room** (`4. Live War Room`) and the **Strategic Executive Briefing** (`6. Results & Chatbot`).
    - Clicking `1. Scenario` opens a detailed popover modal displaying the exact hypothesis and seed announcement memo.
    - Right-side action controls dynamically adapt to simulation state (e.g. `Conclude Early` while simulating, `Ask Analyst / QA` and `New Rehearsal` on report, and `[X]` to return to overview).
  - **Sticky Top-0 Pinning**: The bar remains permanently pinned at the top of the viewport as users scroll through large war room feeds or long executive reports.

---

### v1.10.10-Kiwi (2026-09-11)

- **Real-Time Price & Resource Estimator Next to Launch Button (`src/components/sai/CreateRehearsalView.tsx`)**:
  - **Live Dynamic Price Estimator Widget**: Added a cost & token calculation pill right next to the "Proceed to Token Estimation & Launch" button in the bottom action bar.
  - **Instant Formula Reactivity**: Dynamically updates projected costs and token volume instantly when altering Swarm Scale (15 / 30 / 60 agents), Simulation Rounds (5–20 rounds), Diurnal Cycle toggle, or Intelligence Model (`gpt-5.6-luna`, `gpt-5.6-terra`, `deepseek-chat`, `gpt-4o-mini`).
  - **Interactive Micro-Interaction**: Clicking the price estimator pill immediately opens the pre-flight resource breakdown modal with comprehensive quota analysis and call counts.
  - **Demo Mode Awareness**: In Demo Mode, cleanly shows `€0.000` with an emerald `Demo Free` indicator.

---

### v1.10.9-Kiwi (2026-09-11)

- **CRM Grounding Sources: Uploaded Files & Dynamic Unified Entries (`src/components/sai/CreateRehearsalView.tsx`, `SaiModule.tsx`, `App.tsx`, `api/Swarm/CrmContextExtractor.php`)**:
  - **Uploaded Commercial Files & Documents**: Added a dedicated grounding data source for CRM files (contracts, price proposals, signed agreements, invoices, and attachments from lead timelines).
  - **Dynamic Unified Entries Ingestion**:
    - Automatically discovers and displays any active custom Unified Entry registries created in the CRM (e.g. `Evidencia`, Certificates, Equipment Assets, Custom Modules).
    - When unified entries exist, they dynamically appear as selectable cards in the CRM Grounding Data Sources grid with custom titles, entry names, and registry badges.
    - If none are configured or archived, the grid cleanly excludes them.
  - **Backend Context Extraction Engine Upgrade (`CrmContextExtractor.php`)**:
    - Implemented `extractFilesAndDocuments()` to query commercial contracts and document files across timelines.
    - Implemented `extractUnifiedEntries()` to safely inspect `unified_entries` and query active `ue_{safeId}` tables for titles, filenames, and client links.
    - Fixed column mapping from `te.created_at` to `te.timestamp` in `timeline_events` queries, resolving previous silent exceptions.

---

### v1.10.8-Kiwi (2026-09-11)

- **Contextual Input Explanations for Rehearsal Creation (`src/components/sai/CreateRehearsalView.tsx`)**:
  - **Comprehensive Guidance Under Every Input Field**: Added explicit, beautifully styled guidance blocks under every parameter on the Rehearsal Creation screen, answering both **What it is for** and **How it is used**:
    1. **Rehearsal Title**: Executive identifier used across dashboards, checkpoint saves, war room telemetry, and exported PDF reports.
    2. **Strategic Hypothesis / What-If Variable**: Core strategic pivot or pricing question injected into persona contexts as the central debate prompt.
    3. **Seed Scenario & Announcement Text**: Word-for-word source memo/release that personas read in Round 1 to form sentiment and quote specific clauses.
    4. **CRM Grounding Data Sources**: Real-world CRM databases selected to mine past objections and calibrate persona biases in the knowledge graph.
    5. **CRM Lookback Horizon**: Historical timeframe window (6 vs 12 vs 24 months) used to balance recent market trends with multi-year renewal patterns.
    6. **Swarm Scale (Agents)**: Persona population size determining statistical breadth, edge-case discovery, and network herd dynamics.
    7. **Simulation Rounds**: Iterative temporal wave depth showing whether initial customer outrage normalizes or leads to churn.
    8. **Intelligence Model**: LLM cognitive engine selection (GPT-5.6 Luna for speed and cost efficiency vs. GPT-5.6 Terra for deep reasoning).
    9. **Diurnal Sleep Cycle**: Circadian night pauses that cool off transient emotional outrage between simulated business days.

---

### v1.10.7-Kiwi (2026-09-11)

- **Dedicated Deep-Link URL Routing for Every View & Subview (`src/App.tsx`, `src/components/sai/SaiModule.tsx`, `src/components/AutomationView.tsx`)**:
  - **Full URL Mapping for All SAI Subviews**:
    - `#sai` / `#sai/list`: Main rehearsals list and past runs dashboard.
    - `#sai/create`: Standalone full-screen creation view for new rehearsals.
    - `#sai/create?draftId=<id>`: Deep link directly into an in-progress draft rehearsal.
    - `#sai/war-room?id=<id>`: Dedicated link to Live War Room with simulation state and real-time social feed.
    - `#sai/report?id=<id>`: Dedicated executive briefing and strategic report view.
    - `#sai/demo`: Direct deep link that automatically opens the animated Guided Process Walkthrough.
  - **Bidirectional Browser History Synchronization**:
    - Integrated clean `window.location.hash` updating on all view transitions, cards, buttons, and switches.
    - Added `hashchange` listeners with recursion guards (`isInternalHashUpdateRef`) enabling full browser Back and Forward button support, reload persistence, and direct bookmarking.
  - **Automation Sub-Tab URL Routing**:
    - Added sub-path routing support for `#automation`, `#automation/editor` (new workflow), `#automation/editor?id=<id>` (specific workflow editor), `#automation/logs?id=<id>` (execution logs), and `#automation/settings`.
    - Preserved parent sidebar navigation highlighting across all sub-tab routes.

---

### v1.10.6-Kiwi (2026-09-11)

- **Light-Themed Knowledge Graph & Elevated Node Graphics (`src/components/sai/SwarmGraphCanvas.tsx`, `GuidedDemoWalkthrough.tsx`)**:
  - **Premium Light Canvas Aesthetics**: Converted the Knowledge Graph from pitch-black (`bg-slate-950`) to a crisp light theme with a slate dot matrix pattern (`#94a3b8` at 32% opacity), subtle intelligence radar/orbit rings, and centered crosshair guidelines.
  - **Multi-Layered 3D Node Spheres**:
    - Replaced flat solid circles with linear gradient fills tailored to each stakeholder type (Sapphire for Client, Crimson for Competitor, Amber for Regulator, Purple for Agency, Emerald for Stakeholder).
    - Added specular gloss highlight arcs, layered drop shadows (`feDropShadow`), and double-ring concentric halos on active and selected nodes.
  - **Category Glyph Icons**: Rendered vector glyph icons inside each node orb (Enterprise building for Client, Lightning zap for Competitor, Shield for Regulator, Sparkles for Agency, Group network for Stakeholder).
  - **Pill Label Badges**: Redesigned truncated raw text into elevated floating white pill badges with category accent dots, rounded borders, and sharp dark slate typography (`text-slate-900` / `#0f172a`).
  - **Enhanced Edge & Relation Badges**: Rendered directional arrow markers (`marker-end`) on edges and floating midpoint relational pill badges with clear uppercase typography.
  - **Light Popover Card**: Refactored the entity explanation popover and directional caret arrow into a crisp white glass card (`bg-white/98 border border-slate-200 shadow-2xl text-slate-800`).
  - **Category Legend**: Added an unobtrusive category chip legend in the bottom-left corner of the canvas.

---

### v1.10.5-Kiwi (2026-09-11)

- **Constrained In-Card Feed Scrolling & Card-Style Post Items (`src/components/sai/SocialFeedStream.tsx`, `LiveWarRoom.tsx`, `SaiModule.tsx`, `GuidedDemoWalkthrough.tsx`, `QaAssistantDrawer.tsx`)**:
  - **Internal Card Scrolling**: Added explicit `min-h-0` flexbox constraints and `overflow-hidden` across the Live War Room grid (`lg:grid-cols-12`), column wrappers, and `SocialFeedStream` container, ensuring the social stream strictly scrolls inside its card container rather than expanding the outer page.
  - **Card-Style Feed Post Items**: Redesigned each individual post into a structured card with elevated hover state (`bg-slate-50/70 hover:bg-white border border-slate-200/80 rounded-2xl shadow-xs transition duration-200`), improved spacing, clear typography, and formatted engagement metrics.
  - **Guided Demo Walkthrough Enhancements**: Applied the identical height constraints (`h-[500px] min-h-0 overflow-hidden`) and card-styled stream items to Step 3 (Persona Ingestion Stream) and Step 4 (Autonomous Social Stream), ensuring smooth internal scrolling during guided demonstrations.
  - **Viewport Fit Calibrations**: Added `shrink-0` to HUD and progress bars in the Live War Room, and added `overflow-hidden flex flex-col min-h-0` to the main module container in running view.

---

### v1.10.4-Kiwi (2026-09-11)

- **Interactive Knowledge Graph Entity Explanation Popover (`src/components/sai/SwarmGraphCanvas.tsx`)**:
  - **Direct Above-Entity Popover**: Clicking any stakeholder entity in the Knowledge Graph Canvas now displays a dedicated popover card positioned directly above the clicked node, complete with a directional caret pointer.
  - **Rich Contextual Explanation**: Shows entity name, category badge, entity type color accent, full summary explanation of the entity's role/bias, and active graph relations (e.g. `REGULATES → Target`).
  - **Dynamic Viewport Flipping & Clamping**: Automatically flips below the node if positioned near the top boundary of the canvas (`y < 125px`), with horizontal clamping to prevent clipping at canvas edges.
  - **Connected Graph Focus**: Dynamically highlights edges and neighboring entities connected to the selected entity while dimming unrelated nodes, improving topological readability.
  - **Intuitive Dismissal**: Clicking the close button, clicking the entity again, or clicking empty space on the graph canvas instantly closes the popover.

---

### v1.10.3-Kiwi (2026-09-11)

- **Sticky Interrogation Chatbot Card on SAI Results (`src/components/sai/StrategicReportView.tsx`, `SaiModule.tsx`, `QaAssistantDrawer.tsx`)**:
  - **Unbroken CSS Sticky Flow**: Resolved nested overflow boundaries (`overflow-visible` on the main report container) that previously prevented `position: sticky` from engaging against the outer scrolling workspace.
  - **Calibrated Viewport Docking**: Configured the Chatbot Card column with `lg:sticky lg:top-[88px] self-start z-20`, allowing it to dock cleanly 12px below the sticky SAI header and remain permanently visible alongside lengthy multi-section reports.
  - **Adaptive Height Calculation**: Set responsive height constraint (`h-[calc(100vh-210px)] min-h-[500px] max-h-[820px]`) ensuring the interrogation input and send button stay fully visible within the viewport across laptop and desktop displays.
  - **Internal Independent Scroll**: Preserved smooth internal scrolling for the chat message history while scrolling through the main strategic report independently.

---

### v1.10.2-Kiwi (2026-09-11)

- **Configurable CRM Grounding Sources & Selective Ingestion (`src/components/sai/CreateRehearsalView.tsx`, `SaiModule.tsx`, `CrmContextExtractor.php`, `SwarmManager.php`, `checkpointClient.ts`, `crmContextService.ts`)**:
  - **Granular Source Selection Controls**: Added an interactive CRM Grounding Sources control panel on the Rehearsal Creation view, enabling users to selectively toggle individual CRM repositories on or off before launching a rehearsal.
  - **6 Modular Grounding Sources Supported**:
    1. `active_leads`: Active Pipeline Leads (in-progress deals, discovery accounts, unclosed opportunities).
    2. `existing_clients`: Existing & Won Retainers (active accounts, retainer clients, won contracts).
    3. `lost_deal_objections`: Lost Deals & Objections (historical sales resistance, price sensitivity, rejection reasons).
    4. `competitor_intel`: Competitor Mentions & Intel (rival platform references, competitor pricing discussions).
    5. `meeting_notes`: Meeting Transcripts & Discovery Notes (verbal feedback, discovery call transcripts).
    6. `client_emails`: Client Inbound Emails (inbound inquiries, scope discussions, email correspondence).
  - **Bulk Actions & Visual Badges**: Provided "Select All" and "Clear All" convenience toggles, dynamic status counters (`X of 6 Active`), active ring highlights, and source count badges on rehearsal history cards.
  - **Zero-Source Safeguard**: Added fallback handling and visual warning banner when all sources are disabled, allowing pure scenario grounding based solely on the user's seed memo.
  - **Draft State Persistence**: Selected CRM data sources are stored in the server checkpoint state and cleanly restored when reopening or editing saved rehearsal drafts.

---

### v1.10.1-Kiwi (2026-09-11)

- **SAI Full-Page Rehearsal Creation View & Draft Management (`src/components/sai/CreateRehearsalView.tsx`, `SaiModule.tsx`, `checkpointClient.ts`, `SwarmManager.php`)**:
  - **Full-Page Creation View**: Replaced the popup modal wizard (`SimulationWizard`) with a dedicated, spacious full-page screen view (`activeView === 'create'`) featuring top breadcrumbs, back-navigation, quick scenario templates, and integrated preflight token cost estimation.
  - **Draft Creation & Resumption**: Added support for saving unfinished rehearsals as drafts at any point with real-time save timestamp feedback. Drafts are highlighted with an amber `DRAFT` badge in the rehearsals list, and can be resumed, modified, launched, or deleted.
  - **Updated Intelligence Models**:
    - Configured **`gpt-5.6-luna`** as the default primary high-speed & cost-optimized engine.
    - Configured **`gpt-5.6-terra`** as the heavy cognitive reasoning engine for complex market simulations.
    - Updated `PreflightEstimatorModal` with real-time quota estimations for both GPT-5.6 Luna and GPT-5.6 Terra.

---

### v1.10.0-Kiwi (2026-09-11)

- **Swarm Artificial Intelligence (SAI) Engine (`api/swarm.php`, `src-php/Swarm/`, `src/utils/swarm/`, `src/components/sai/`)**:
  - **Hybrid Client-Orchestrated Architecture**:
    - Built a fully autonomous, multi-agent predictive simulation engine running in browser runtime (React 19 / TypeScript) while persisting snapshots to PHP 8 / MySQL.
    - Eliminates Python, Docker, Celery, and Redis dependencies from customer environments.
    - Integrated Screen Wake Lock API and `beforeunload` protection to prevent machine sleep or accidental tab closing during multi-round rehearsals.
  - **Dynamic Database Sharding & Checkpointing (`src-php/Swarm/SwarmManager.php`, `src/utils/swarm/checkpointClient.ts`)**:
    - Dynamic per-simulation table sharding (`sim<sim_id>_nodes`, `sim<sim_id>_edges`, `sim<sim_id>_agents`, `sim<sim_id>_posts`) prevents index bloat and ensures high-throughput writes.
    - Round-by-round asynchronous JSON snapshot checkpointing allows instant recovery and zero data loss on browser crash.
    - Atomic table dropping on deletion guarantees zero orphaned records.
  - **Temporal CRM Grounding (`src-php/Swarm/CrmContextExtractor.php`, `src/utils/swarm/crmContextService.ts`)**:
    - Injects real CRM leads, client interactions, and historical lost deal objections into the simulation seed.
    - Configurable lookback horizon (6, 12, or 24 months) prevents 10-year stale data contamination.
  - **Native Multi-Factor Linear RecSys Algorithm (`src/utils/swarm/recsysEngine.ts`)**:
    - Native TypeScript / PHP linear recommendation formula ($0.4 \cdot \text{Recency} + 0.3 \cdot \text{Popularity} + 0.3 \cdot \text{Relevance}$) dynamically populates candidate feeds for each agent turn.
  - **Secure Server-Side LLM Proxy (`api/swarm.php`)**:
    - Proxies all LLM completions via authenticated server endpoints, decrypting CCRM's existing system OpenAI keys (`INTEGRATIONS_CONFIG`) without exposing raw credentials to the client.
  - **Mandatory Pre-Flight Token & Cost Estimator (`src/components/sai/PreflightEstimatorModal.tsx`)**:
    - Pre-flight modal calculates projected token volume and API cost before simulation start, requiring user acknowledgment to prevent unexpected charges.
  - **Live War Room & Visualizations (`src/components/sai/LiveWarRoom.tsx`, `SwarmGraphCanvas.tsx`, `SocialFeedStream.tsx`)**:
    - Real-time animated SVG knowledge graph showing active stakeholder nodes and posting ripples.
    - Cascading dual-platform (Twitter/X & Reddit) social feed stream with real-time sentiment tracking and circadian diurnal pacing clock.
  - **Executive Strategic Briefing & Playbook (`src/components/sai/StrategicReportView.tsx`, `src/utils/swarm/reportSynthesizer.ts`)**:
    - ReAct Chief Intelligence Analyst generates comprehensive post-simulation analysis.
    - Includes mandatory chapter: *"What Strategy to Use to Achieve the Goal?"* with actionable countermeasures, competitor retaliation analysis, and sales objection rebuttals.
  - **Interactive Interrogation Drawer (`src/components/sai/QaAssistantDrawer.tsx`, `src/utils/swarm/chatAssistant.ts`)**:
    - Slide-out drawer for multi-turn Q&A with the Chief Analyst or direct 1-on-1 cross-examination of any simulated agent persona.
  - **Interactive Demonstration Mode & Zero-Quota UX Evaluation (`src/utils/swarm/demoData.ts`, `src/components/sai/SaiModule.tsx`)**:
    - Added a dedicated Demo Mode toggle in the top navigation bar and full synchronization with system-wide `isDemoMode`.
    - Integrated pre-computed Enterprise Pricing Rehearsal demo dataset (+25% increase, 99.9% uptime SLA, WhatsApp developer bridge) for instantaneous exploration with zero API tokens consumed.
    - Simulated fast local round progression with realistic agent turn pacing, live graph ripples, circadian clock tracking, and ReAct strategic briefing compilation.
    - Added contextual simulated interrogation in the Q&A Drawer for both the Chief Intelligence Analyst and 1-on-1 interviews with specific agent personas (Sarah Jenkins, Marcus Vance, David Chen, Dr. Elena Rostova).
  - **Navigation & Brand Identity (`src/components/icons/FlockIcon.tsx`, `src/components/Sidebar.tsx`, `src/components/StartMenu.tsx`, `src/App.tsx`)**:
    - Added top-level navigation item labeled **"SAI"**.
    - Designed custom vector `FlockIcon` depicting a flying flock of birds in dynamic V-formation with a rich purple-to-emerald gradient (`from-purple-600 via-indigo-500 to-emerald-400`).
