# Update 1.8 (Codename: Imbe)

This document tracks all new features, architectural changes, schema migrations, and improvements introduced in the 1.8 Imbe update cycle.

---

### v1.8.27-Imbe (2026-08-26)

- **RAG AI Assistant Backend Fix (`public/api/chat_rag.php`)**:
  - **Syntax Error Fix**: Resolved an unclosed `try-catch` block in `chat_rag.php` around the Unified Entries RAG lookup that caused PHP fatal parse errors and JSON decode failures on client requests.
  - **Dynamic Backend Codename Sync**: Updated backend system prompt generation to dynamically resolve the current version codename (`Imbe`) rather than a hardcoded persona name.

### v1.8.26-Imbe (2026-08-26)

- **Dynamic RAG AI Assistant Naming (`src/utils/version.ts`, `src/components/RagAiView.tsx`)**:
  - **Automated Version Codename Syncing**: Bound the default built-in RAG AI assistant persona (`STOCK_DEFAULT_AGENT`) directly to `VERSION_CODENAME` ("Imbe"), ensuring the assistant's name, system prompt persona, and greetings automatically reflect the current release cycle.
  - **Dynamic Version Badge**: Updated the built-in assistant card badge to dynamically display `v{VERSION}` (`v1.8.26-Imbe`).
  - **Legacy Name Migration**: Added migration logic so existing user preference records with legacy default names automatically adopt the active version codename.

### v1.8.25-Imbe (2026-08-26)

- **Unified Entry Client & Lead Search Filter Fix (`src/components/UnifiedEntryView.tsx`)**:
  - **Comprehensive Client & Lead Matching**: Removed the restrictive `lead.status !== "accepted"` / `lead.status === "accepted"` filter conditions on client and lead search dropdowns in folder creation, entry creation, and detail views.
  - **Multi-Field Search**: Expanded client search to match against client name, city, email, phone, company ID, and contact person so all clients in the workspace are immediately discoverable.
  - **Ergonomic Enhancements**: Displays city alongside email and phone in client suggestions, and ensures input clear actions reset linked IDs properly.

### v1.8.24-Imbe (2026-08-25)

- **Update Notes Localization Fallback Fix (`src/components/Header.tsx`, `src/components/UpdateNotesView.tsx`)**:
    - **Corrected Default Fallback**: Fixed an issue where the update notes viewer would incorrectly fall back to the English (`en`) site handle if the system language (`sk`) was not found in Craft CMS. The fallback now correctly maps `sk` to the `default` site handle, ensuring localized images and content from the primary CMS site are properly fetched and rendered.

### v1.8.23-Imbe (2026-08-25)

- **Windows-Style Start Menu Navigation & Custom Workspace Layouts (`src/components/StartMenu.tsx`, `src/components/Sidebar.tsx`)**:
    - **Interactive Slide-Up Start Menu**: Added a centralized Start Menu accessible via the 'START' navigation trigger with categorized application columns (Operations, Analytics, Collaboration, System, and Custom groups).
    - **Interactive Edit Mode**: Drag-and-drop reordering of navigation cards with real-time landing indicators, full-width item headers, and dynamic group creation/renaming/deletion.
    - **Pin to Sidebar System**: Allows users to pin/unpin any module directly to the sidebar navigation. Fresh installations default to a clean, empty sidebar until modules are pinned.
- **Financial Management & Cash Flow Engine (`src/components/FinancialManagementView.tsx`, `api/schema.php`, `sync.php`)**:
    - **Cash Flow Tracking**: Comprehensive multi-tab financial command center featuring live inflow vs. outflow analytics, category breakdown matrix tables, and ledger movements.
    - **3-Level Hierarchical Categories & Recurring Rules**: Support for multi-tiered financial categorization and automated recurring expense/revenue projections.
    - **Database Persistence**: Implemented `financial_records` and `financial_categories` schema tables with bi-directional client-server delta synchronization via `sync.php`.
- **Warehouse Persistence & Data Integrity Polish (`src/components/WarehouseView.tsx`, `api/registeruz.php`)**:
    - **Stock Balance & Movement Persistence**: Resolved database persistence issues for warehouse movements, stock adjustments, and multi-warehouse inventory balances.
    - **UI & Styling Refinements**: Fixed input layout glitches, alignment issues, and enhanced RegisterUZ business registry lookup integration.
- **Agent Memory & Versioning Automation (`.agents/rules/commit-and-versioning.md`, `GEMINI.md`)**:
    - Established project rule enforcing atomic git commits for significant changes and version bumping (`one build = one version bump`).

---

### v1.8.22-Imbe (2026-08-18)

- **Goods Issue Table Input UX & Ergonomics (`src/components/WarehouseView.tsx`)**:
    - **Removed Stepper Buttons**: Removed `-` and `+` increment/decrement buttons from the Quantity column for a cleaner and more direct data entry flow.
    - **Wider Input Fields**: Expanded the Quantity column width (`w-36 min-w-[130px]`) with the unit badge anchored neatly inside the field (`pr-9`).
    - **Smooth Retyping & Zero-Reset Prevention**: Numeric fields (`quantity`, `discountPct`, `baseSellPrice`, `unitSellPrice`) now retain empty values when backspaced or cleared, allowing effortless retyping of numbers without snapping back to `0`.

---

### v1.8.21-Imbe (2026-08-18)

- **Goods Issue Table Empty-State Refinement (`src/components/WarehouseView.tsx`)**:
    - **Removed In-Table Button**: Removed the "+ Add Line Item" button from inside the table's empty state.
    - **Search Bar Guidance Note**: Replaced with a clear, concise instruction informing the user to use the top search bar to search and add products row by row.

---

### v1.8.20-Imbe (2026-08-18)

- **Empty-State Starting Flow for New Goods Issues (`src/components/WarehouseView.tsx`)**:
    - **Zero Pre-Populated Rows on New Issues**: Opening a new Goods Issue starts with a completely clean table (`issueItems: []`), giving the user full control to search and add products row by row.
    - **Contextual Product Prefill**: Prefilling a specific product now only occurs when explicitly clicking _"Issue (VYD)"_ directly from that product's detail page.
    - **Friendly Table Empty State**: Added an empty state illustration with "+ Pridať položku" button and guidance text whenever the issue list has no rows.
    - **Full Row Deletion**: Allowed removing rows all the way down to 0 items.

---

### v1.8.19-Imbe (2026-08-18)

- **Dedicated Full-Page Goods Issue (Výdajka - VYD) & Direct Client Sale Attachment**:
    - **Dedicated Full-Screen Workspace (`src/components/WarehouseView.tsx`)**:
        - Replaced the cramped popup modal with an expansive full-page view accessible directly via `#warehouse/issue/new` or `#warehouse/issue`.
        - Sticky top navigation bar with breadcrumbs (`Sklad / Pohyby & Výdajky / Nová výdajka`), document badge (`VYD-2026-XXXX`), status pill, and action buttons (`Zrušiť`, `Potvrdiť výdajku a odpísať zo skladu`).
    - **Left Narrow Sidebar (Client, Logistics & Financial Summary)**:
        - **Searchable Client Selector**: Fast live combobox to pick clients or leads from CRM with company initials avatar, type, phone, email, and VAT ID preview card. Supports Direct Sale / Unassigned customer.
        - **Logistics & Purpose**: Source warehouse dropdown, auto-generated document number, issue date picker, and delivery note/purpose textarea.
        - **Live Financial Summary Card**: Dark gradient card showing catalog subtotal, discount total and %, final sale price in large bold typography, total inventory WAP cost, and gross profit margin % badge.
        - **Client Timeline Sale Attachment**: Enabled by default checkbox that automatically creates a sale entry with a formatted items breakdown and total price directly into the selected client's timeline.
    - **Right Main Workspace (Product Autocomplete & Row-by-Row Pricing Grid)**:
        - **Fast Product Search Bar**: Autocomplete search by name, SKU, barcode, or category showing live stock availability pill in the selected warehouse, catalog price, and 1-click addition.
        - **Interactive Line Items Table**:
            - Live stock availability pill with warning alerts if requested quantity exceeds available warehouse stock.
            - FEFO batch lot selector with auto-selection of earliest expiration date for tracked items.
            - Stepper controls for quantity.
            - Interactive discount percentage input with quick preset chips (0%, 5%, 10%, 15%) that recalculates unit sale price and line totals in real time.
            - Direct editable sale price input (automatically updates discount %).
            - Line item notes and row deletion.
            - "+ Add Empty Row" button and table footer summarizing total line items and total monetary value.

---

### v1.8.18-Imbe (2026-08-18)

- **Cleaned Up Product URLs (Eliminated Redundant Prefixes)**:
    - **Streamlined URL Architecture (`src/components/WarehouseView.tsx`, `api/universal_search.php`)**:
        - Cleaned product URLs to `#warehouse/${id}` (e.g. `http://localhost:8085/#warehouse/item-2` instead of redundant `#warehouse/item-item-2`).
        - Updated universal fuzzy search, sticky product header, and catalog table anchors to produce crisp, clean product URLs.
        - Hash listener automatically parses and normalizes `#warehouse/item-2`, `#warehouse/product/item-2`, `#warehouse/new`, and legacy `#warehouse/item-item-2` gracefully.

---

### v1.8.17-Imbe (2026-08-18)

- **Direct Deep-Link URLs & Shareable Product Links**:
    - **Dedicated Product URLs (`src/components/WarehouseView.tsx`)**:
        - Products now have unique, direct deep-link URLs formatted as `#warehouse/item-${id}` (and `#warehouse/new` for the product creator).
        - Synchronized browser URL hash bidirectionally: opening/closing a product or navigating via browser history, bookmarks, or external links instantly loads and auto-populates that product's detail form.
    - **Interactive Links & Copy URL Action**:
        - Added clickable anchor tags (`<a href="#warehouse/item-ID">`) to product titles in the catalog data grid for native right-click, "Open in new tab", and browser bookmarking.
        - Added **Copy URL** (`<Link2 />`) buttons in both the sticky product detail header and the catalog table rows with instant clipboard feedback.

---

### v1.8.16-Imbe (2026-08-18)

- **Adaptive Compact Sticky Header on Scroll**:
    - **Dynamic Stuck State & Narrower Layout (`src/components/WarehouseView.tsx`)**:
        - Integrated real-time scroll observer on the workspace viewport to toggle compact styling (`isHeaderStuck`).
        - When scrolled and stuck to the top:
            - Contracts vertical padding from `py-3.5 md:py-4` down to `py-2 md:py-2.5` with crisp `shadow-md`.
            - Condenses product title from `text-xl md:text-2xl` to `text-base md:text-lg` and hides redundant top breadcrumb paths.
            - Miniaturizes badges (`text-[10px]`), icon buttons (`p-1.5 md:p-2`), and action controls (`py-1.5 px-3.5`).
            - Smooth `transition-all duration-200` ensures a fluid animation between full and compact states.

---

### v1.8.15-Imbe (2026-08-18)

- **Flush Top-Pinned Sticky Navigation Bar**:
    - **Seamless Top Header Docking (`src/components/WarehouseView.tsx`)**:
        - Positioned the Product Detail action and navigation bar right against the top of the viewport under the main header (`sticky -top-4 md:-top-6 z-40 -mt-4 md:-mt-6 -mx-4 md:-mx-6 px-4 md:px-6 py-3.5 md:py-4 bg-white/95 backdrop-blur-md border-b border-slate-200/80 shadow-sm`).
        - Eliminates dead whitespace above the toolbar, spanning full width and seamlessly sticking flush directly under the primary app header bar when scrolling through product statistics, warehouses, and movements.

---

### v1.8.14-Imbe (2026-08-18)

- **Warehouse Products Added to RAG DB Training, Vector Ingestion & Search**:
    - **Vector DB Training & Ingestion Pipeline (`api/train_vector.php`)**:
        - Added warehouse products/materials to the vector training stats API (`productsCount`) and ingestion generator.
        - Parses product names, SKUs, EAN/barcodes, multi-categories, unit, selling prices, WAP purchase costs, physical/available/reserved stock balances, bins, FEFO tracking state, active batches, and technical specifications into vectorized training chunks.
    - **RAG AI Assistant Context Retrieval (`api/chat_rag.php`, `api/agent_utils.php`)**:
        - Integrated live warehouse products and inventory balances into the RAG context blocks for interactive AI chat and autonomous agent background runs.
        - LLM now accurately answers queries regarding product specifications, current stock levels, lot expirations, unit pricing, and inventory valuations.
    - **Universal Fuzzy Search Integration (`api/universal_search.php`, `src/components/Header.tsx`)**:
        - Added Section 5 for warehouse items fuzzy searching across names, SKUs, barcodes, categories, storage bins, and descriptions.
        - Displays product badges, SKU info, live stock count, and price in header search dropdown with instant routing (`#warehouse/item-ID`).
    - **Settings RAG Training UI (`src/components/SettingsView.tsx`)**:
        - Added **Products / Stock (Skladový tovar)** stat tile in the RAG Knowledge Index Ingestion overview.

---

### v1.8.13-Imbe (2026-08-18)

- **Sticky Navigation & Actions Header Bar**:
    - **Product Detail Sticky Top Bar (`src/components/WarehouseView.tsx`)**:
        - Styled the top breadcrumb and actions bar (Back button, Title, SKU & Category badges, Stock status pill, and Cancel / Delete / Save Changes buttons) with `sticky top-2 z-40 bg-white/95 backdrop-blur-md shadow-md`.
        - Ensures quick navigation and instant saving access when scrolling through lengthy inventory details, stock tables, and movement logs.

---

### v1.8.12-Imbe (2026-08-18)

- **Adaptive Warehouse Stocks Layout (Table for Non-FEFO vs Map for FEFO)**:
    - **Compact Stock Balances Table (`src/components/WarehouseView.tsx`)**:
        - When a product does not have expiration tracking active (`!hasExpiration`), Tab 2 renders a clean, compact **Warehouse Stock Balances Table** instead of individual cards with empty batch maps.
        - Displays Warehouse Name & Code, Bin/Location, Physical Stock, Reserved Quantity, Available Stock to sell, and Inventory Valuation at WAP, complete with a total summary footer row.
    - **Dynamic FEFO Batch Map Layout**:
        - When expiration tracking is enabled (`hasExpiration`), Tab 2 renders the full multi-warehouse card layout with active FEFO expiration countdowns and batch health maps.
    - **Adaptive Tab Label**:
        - Button switches between _Stavy na skladoch_ (Warehouse Stock) and _Sklady a FEFO mapa_ (Warehouses & FEFO Map) depending on the product's tracking state.

---

### v1.8.11-Imbe (2026-08-18)

- **Dedicated Profitability Calculator Card & Inventory Performance Projection**:
    - **Standalone Profitability Calculator Card (`src/components/WarehouseView.tsx`)**:
        - Re-architected the Profitability Calculator in Tab 1 into its own dedicated, high-visibility card.
        - Features Gross Profit per unit, Commercial Gross Margin % (with coefficient), Markup % over cost, margin health pill, and full Price Composition distribution bar (WAP cost share vs Gross profit share).
    - **Inventory Revenue Potential Card**:
        - Added projected total revenue and gross profit metrics upon complete liquidation of physical stock at the suggested sale price.

---

### v1.8.10-Imbe (2026-08-18)

- **Product Detail Workspace Reorganization & Streamlined Financials**:
    - **Promoted Stats Overview Bar to Top of View (`src/components/WarehouseView.tsx`)**:
        - Relocated the **Warehouse Trend & Inventory Analytics Bar** (Total Stock on Hand, Valuation at WAP, 30-Day Purchases Inflow, 30-Day Sales Outflow, and Flow Trajectory & Reorder Health Bar) directly beneath the top breadcrumb header.
        - Key inventory metrics are now persistently visible across all active workspace tabs (`statistics`, `warehouse`, `movements`).
    - **Streamlined Tab 1 (Profitability & Margin Analysis)**:
        - Removed redundant `Average Purchase Price (WAP)` and `Suggested Sale Price` input and summary boxes from the Financials card.
        - Replaced with a focused, premium **Profitability & Margins Widget**:
            - Real-time Gross Profit per unit (`+unitProfit € / MJ`).
            - Commercial Gross Margin % (`marginPct%`) and margin coefficient.
            - Markup over purchase cost % (`markupPct%`).
            - High/Standard/Low margin status indicator.
            - Visual Price Composition Distribution bar illustrating cost share vs gross margin.

---

### v1.8.9-Imbe (2026-08-18)

- **Multi-Category Assignment Support for Products**:
    - **Data Model & Type System (`src/types/index.ts`)**:
        - Expanded `WarehouseItem` interface to support multiple category tags via `categories?: string[]` while maintaining full backwards compatibility with `category?: string`.
    - **Multi-Category Combobox with Checkboxes & Dynamic Creation (`src/components/WarehouseView.tsx`)**:
        - Redesigned the Category section in the left sidebar into an interactive multi-select combobox.
        - Displays selected categories as compact dismissible badges (`[Kategória] x`).
        - Dropdown offers instant live search, non-closing toggle checkboxes for multiple category selection, and dynamic option addition (`+ Pridať a vybrať`).
    - **Catalog & Inventory Multi-Category Filtering**:
        - Items assigned to multiple categories match under any of their assigned categories in category dropdown filters, text searches, and analytics valuation charts.
        - Catalog data grid and Product Detail header breadcrumbs display all category badges associated with each item.

---

### v1.8.8-Imbe (2026-08-18)

- **3-Tab Right Workspace in Product Detail & Rapid Purchase/Sale Modals**:
    - **Tab Selector Navigation (`src/components/WarehouseView.tsx`)**:
        - Implemented a 3-tab navigation bar on the right side of the dedicated Product Detail view:
            1. **`statistics` (Štatistiky a Cenotvorba)**:
                - 4 Key Metric Badges: Total Stock on hand, Total Inventory Valuation at WAP, 30-Day Inflow (Purchases), 30-Day Outflow (Sales).
                - Stock Velocity & Reorder Health Bar with percentage of optimal inventory.
                - Pricing, WAP Costing & Live Profitability Calculator (Gross profit, Gross margin %, Markup %, Status badge).
            2. **`warehouse` (Sklady a FEFO mapa)**:
                - Multi-warehouse balances (Physical, Reserved, Available) broken down by location.
                - **Dedicated Per-Warehouse FEFO Map**: For each individual warehouse, displays its active batches with batch number, location, expiration countdown, status semaphores (`OK`, `Warning < 30d`, `Expired`), available lot quantity, and unit purchase cost.
            3. **`movements` (Pohyby tovaru)**:
                - Top Action Bar with direct transaction buttons:
                    - `+ Zaznamenať nákup (PRI)`: Quick inward receipt logging.
                    - `- Zaznamenať predaj (VYD)`: Quick outward issue logging.
                - Filtered chronological stock movement audit trail for this product with print voucher actions.
    - **Joint Partner & Client Searchable Comboboxes**:
        - Built unified searchable selectors combining Suppliers (`suppliers`) and Clients/Leads (`leads`) with distinct `[Partner]` and `[Klient]` badges for seamless transaction logging.
    - **Searchable FEFO Expiration Date & Batch Selector for Sales**:
        - In the Sale Issue modal, active batches in the selected warehouse are sorted chronologically by expiration date (FEFO priority) with instant search filtering, expiration countdown badges, and quantity previews.

---

### v1.8.7-Imbe (2026-08-18)

- **Stock Thresholds & Targets Relocation**:
    - Relocated the **Stock Thresholds & Targets** card (Min Stock Alert and Optimal Target Stock) in [src/components/WarehouseView.tsx](file:///Users/erik/Documents/vibe%20coding/crm/src/components/WarehouseView.tsx) to the **Left Sidebar Column**, placed directly underneath the Fixed Product Data card.
    - Integrated safety lock/unlock binding (`isProductCardLocked`) to the threshold inputs to safeguard reorder limits against accidental tampering.
    - Streamlined the right column layout to focus exclusively on Financials & Live Profitability, Multi-Warehouse Distribution, Batches/FEFO, and Movement Timelines.

### v1.8.6-Imbe (2026-08-18)

- **Hold-to-Unlock Protection for Fixed Product Data**:
    - Added a security & safety **Lock/Unlock mechanism** to the Left Sidebar Card in [src/components/WarehouseView.tsx](file:///Users/erik/Documents/vibe%20coding/crm/src/components/WarehouseView.tsx).
    - **Hold-to-Unlock Interaction**:
        - To prevent accidental modifications to fixed product attributes (Name, Photo, Suggested Sale Price, EAN/Barcode, SKU, Category, Unit, Location, Expiration, Description), the card defaults to a locked state for existing catalog items.
        - Unlocking requires pressing and **holding the lock button for 1 second** (`1000ms`), featuring a live filling progress animation overlay and percentage countdown. Releasing before 1 second cancels the unlock action.
        - Once unlocked, fields become editable, and clicking the button instantly re-locks the fixed data.
    - Added disabled states, locked styling, and toast notifications on successful unlock.

### v1.8.5-Imbe (2026-08-18)

- **Direct Image File Uploader (Replaced Manual Image URL)**:
    - Replaced the manual text `Image URL` input in [src/components/WarehouseView.tsx](file:///Users/erik/Documents/vibe%20coding/crm/src/components/WarehouseView.tsx) with an interactive **drag-and-drop & click-to-upload file picker**.
    - Integrated direct image uploading via the backend `/upload.php` endpoint with automatic magic-number format verification (PNG, JPG, WEBP, SVG) and storage in `/uploads/`.
    - Added instant client-side image preview, loading spinner overlay during upload, "Zmeniť foto" action button, and a quick-remove button (`X`).

### v1.8.4-Imbe (2026-08-18)

- **Pricing & WAP Costing Streamlining**:
    - Cleaned up the right column Financials Card in [src/components/WarehouseView.tsx](file:///Users/erik/Documents/vibe%20coding/crm/src/components/WarehouseView.tsx).
    - Maintained the primary editable input for Suggested / Default Selling Price solely in the left fixed-data sidebar.
    - Replaced the duplicate input in the right column with a styled read-only Selling Price card that pulls live values from the left panel for margin and markup comparison with Average Purchase Price (WAP).

### v1.8.3-Imbe (2026-08-18)

- **Webshop-Style Product Detail Layout & Searchable Dynamic Category Combobox**:
    - **Sidebar Layout Redesign (`src/components/WarehouseView.tsx`)**:
        - Moved fixed product data into a **narrower left sidebar** (`lg:col-span-1`) and primary calculations/balances into the **wider right content area** (`lg:col-span-2`).
        - **Webshop-Style Compact Header Card**:
            - Replaced oversized image view with a compact e-commerce thumbnail (`w-20 h-20`) on the left and the bold Product Name input on the right.
            - Underneath, strictly structured fixed attributes in sequence:
                1. **Suggested Sale Price (excl. VAT)** with large font and currency unit badge (`EUR / m²` / `EUR / ks`).
                2. **EAN / Barcode** with `Barcode` icon and monospace font.
                3. **SKU Code** with `Tag` icon and monospace font.
                4. **Searchable Category Combobox with Dynamic Option Creation**:
                    - Interactive dropdown filter supporting live text search.
                    - Ability to instantly add new custom categories on the fly with the `+ Pridať novú / Add` action or Enter key.
                    - Persists user-defined custom categories and auto-merges with existing catalog categories.
                5. **Unit of Measure (MJ)** dropdown (`ks`, `m²`, `bm`, `m³`, `kg`, `l`, `balenie`, `paleta`).
                6. **Storage Location / Bin** with `MapPin` icon.
                7. **FEFO Expiration Tracking** toggle for perishable products.
                8. **Description** multiline notes.
        - **Right Column Main Area**:
            - Financials, WAP costing & Live Profitability Calculator with gross margin %, profit status badge, and markup over cost.
            - Stock thresholds & min/optimal target levels.
            - Multi-warehouse stock distribution table across all branches.
            - Active FEFO lot batches with live countdown and semaphores.
            - Recent stock movements timeline.

### v1.8.2-Imbe (2026-08-18)

- **Dedicated Full-Page Product Detail & Editor Screen (Replaced Popup Modal)**:
    - **Dedicated 360° Product View (`src/components/WarehouseView.tsx`)**:
        - Replaced the popup modal dialog (`isItemModalOpen`) for Product Creation and Editing with a dedicated full-screen workspace.
        - **Header & Breadcrumbs**: Added navigation breadcrumb (`Sklad / Katalóg tovaru / [Product Name]`), stock status badge (In Stock / Low Stock / Out of Stock), SKU badge, category pill, back button (`← Späť na skladové zásoby`), delete button with confirmation, and primary Save action button.
        - **Left Column (2/3 Workspace)**:
            - _Základné informácie_: Product name, SKU code, Barcode/EAN with icon, Unit of measure picker (`ks`, `m²`, `bm`, `m³`, `kg`, `l`, `balenie`, `paleta`), Category with quick-suggestion pills (_Veľkoformátové dosky_, _Prírodný kameň_, _Stavebná chémia_...), and detailed multiline description.
            - _Cenotvorba, WAP a Kalkulátor ziskovosti_: Average Purchase Price (WAP), Last purchase price, Selling price (excl. VAT), and real-time live profitability widget computing gross profit per unit, gross margin %, and markup over cost with color-coded profit classification badge.
            - _Skladové limity a pozícia_: Default bin/rack position (`MapPin`), minimum alert stock threshold, optimal target stock, and FEFO expiry tracking toggle.
            - _Rozdelenie zásob po skladoch_: Multi-warehouse breakdown table displaying physical on-hand quantity, reserved quantity for orders, available stock, and specific bin position across _Hlavný sklad Bratislava_, _Výrobný sklad Trnava_, etc.
        - **Right Column (1/3 Workspace)**:
            - _Fotografia a vizuál tovaru_: Large high-resolution image preview box with fallback icon and instant URL input.
            - _Rýchle skladové operácie_: One-click shortcuts to create Receipt (_Príjemka - PRI_) or Issue (_Výdajka - VYD_) with this item pre-selected.
            - _Aktívne šarže a exspirácie (FEFO)_: List of active lot batches with expiration dates, quantities, and status semaphores (🟢 OK, 🟡 < 30 days, 🔴 Expired).
            - _História pohybov tovaru_: Timeline of recent inward/outward/transfer warehouse documents affecting this product with document numbers and quantities.
        - **Interactivity**: Clicking any product row or edit button in the main catalog table directly opens the dedicated product view.

### v1.8.1-Imbe (2026-08-18)

- **Warehouse & Inventory Management Module (Complete End-to-End Implementation)**:
    - **Database Architecture & DDL (`api/schema.php`, `public/api/schema.php`, `entities_overview.md`)**:
        - Created 7 relational warehouse tables: `warehouses`, `suppliers`, `warehouse_items`, `warehouse_stock`, `warehouse_batches`, `warehouse_movements`, `warehouse_movement_items`.
        - Implemented strict foreign keys with cascading deletions, indexes on search keys (SKU, barcode, document number, expiration date), and JSON column support for supplier contacts.
        - Documented Section 8 in [entities_overview.md](file:///Users/erik/Documents/vibe%20coding/crm/entities_overview.md) following standard repository guidelines.
    - **Sync Engine & Protocol Extension (`sync.php`, `public/sync.php`)**:
        - Added warehouse tables to data version hashing (`ccrm_compute_data_version`).
        - Implemented GET serialization querying all 7 tables and mapping snake_case DB columns to camelCase TypeScript schemas.
        - Implemented transactional POST handlers for full upserts and delta deletes across warehouses, suppliers, item catalog, stock balances, FEFO batches, and stock movements with nested items.
    - **Seeded Demo Data (`scripts/seed_demo_warehouse.php`)**:
        - Seeded 2 warehouses: _Hlavný sklad Bratislava (WH-BA-01)_ and _Výrobný sklad Trnava (WH-TT-01)_.
        - Seeded 3 B2B suppliers (_Laminam Slovakia s.r.o._, _Stone Import Verona S.r.l._, _Mapei Slovensko s.r.o._) with contact persons and billing data.
        - Seeded 5 stone and chemical products (_Calacatta Gold Quartz_, _Nero Marquina Marble_, _Patagonia Quartzite_, _Keraflex Maxi S1_, _Tenax Ager_).
        - Seeded initial stock levels, FEFO batches with status semaphores, and historical movement documents (`PRI-2026-0001`, `PRI-2026-0002`, `VYD-2026-0001`).
    - **Frontend Architecture & Navigation (`src/types/index.ts`, `src/utils/translations.ts`, `src/components/Sidebar.tsx`)**:
        - Added comprehensive TypeScript interfaces for all warehouse entities.
        - Added full trilingual translations (Slovak, English, Hungarian) in [src/utils/translations.ts](file:///Users/erik/Documents/vibe%20coding/crm/src/utils/translations.ts).
        - Added `#1e3a8a` Navy Blue luxury warehouse item with `Package` icon in [src/components/Sidebar.tsx](file:///Users/erik/Documents/vibe%20coding/crm/src/components/Sidebar.tsx).
    - **Luxury Warehouse Component (`src/components/WarehouseView.tsx`)**:
        - Built master Navy Blue interface with 5 functional tabs:
            1. **Prehľad zásob (Stock Inventory)**: Live stock levels, category filters, low stock warnings, WAP costing, and stock valuation.
            2. **Pohyby a doklady (Movements & Documents)**: Receipts (_Príjemka_ - PRI), Issues (_Výdajka_ - VYD), Transfers (_Prevodka_ - PRE), and Adjustments (_Inventúra_ - INV) with real-time profit tracking.
            3. **Dodávatelia (Suppliers)**: B2B supplier directory with multi-contact cards and live ARES SK/CZ lookup.
            4. **Exspirácie a šarže (FEFO & Batches)**: Expiry tracker with color-coded semaphores (Expired, <30 days, OK).
            5. **Skladová analytika (Warehouse Analytics)**: Stock valuation by category, fast-moving items, and monthly inward/outward volume.
        - Integrated interactive modals: Product catalog creation/editing, Receipt (PRI), Issue (VYD) with Lead timeline auto-logging, Transfer (PRE), Supplier management, and Printable Delivery Note (_Dodací list_).
    - **Application Wiring & Integration (`src/App.tsx`)**:
        - Integrated safe lazy loading, local state hooks, delta sync engine bindings, and routing.
        - Integrated Lead detail timeline linking delivery notes directly to the client's activity feed.
